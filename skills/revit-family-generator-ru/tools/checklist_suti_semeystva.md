# Чек-лист «сути семейства» — заполнить ДО генерации JSON

> Механизм по замечанию BIM-менеджера №3 («не смотрит дальше поставленной задачи»),
> см. `Claude/famfab/gen/gen_schema.md` §Замечания. Копировать этот файл в рабочую
> папку задачи и заполнять по месту. Незаполненный пункт = не готов генерить.

## 0. Референс
- [ ] Реальное фото/чертёж найдены и СВЕРЕНЫ с владельцем («тот тип?») — ссылка/файл: …
- [ ] Паспортные данные производителя добыты (каталог/техлист): …

## 1. Параметры — таблица истины
Для КАЖДОГО параметра одна строка. Правило имён: паспортные — как в паспорте
производителя (D/D2/L у Shuft); в НАШИХ именах не давать ложных намёков на
зависимость («D2» читается как D*2 — если это паспортная величина, tooltip обязателен).

| Параметр | Паспортный или производный? | Истина (каталог/lookup/формула) | В gate.derived? | Tooltip |
|---|---|---|---|---|
| … | … | … | да/— | … |

- [ ] Все производные имеют формулу И строку в `gate.derived` (сверка числом на гейте).
- [ ] instance/type для каждого решён осознанно (формула type-параметра не может
      ссылаться на instance — ловушка №20).

## 2. Поведение
- [ ] Что пересчитывается при изменении КАЖДОГО flex-параметра — прописано: …
- [ ] Края флекса (номинал, +1000 мм, ×0.8): геометрия не рвётся, ничего не пробивает: …
- [ ] Типоразмеры: каталог в rfa / lookup-CSV; `gate.flex_mode` = by_value | by_key: …

## 3. Связность и вид
- [ ] `gate.expected_clusters` осознанно (намеренная несвязность — явным числом >1): …
- [ ] Что видно в Coarse (грубая детализация): …
- [ ] Видимость/галочки (yes_no) и их проверка в гейте (toggle): …

## 4. Спека и MEP
- [ ] Что считается в спецификации; галочка «Специфицировать» (канон шаблона): …
- [ ] Вложения: shared? считаются в спеке? каскад видимость→счёт: …
- [ ] Коннекторы — только у родителя; тип системы, откуда диаметр/размеры: …

## 5. Гейт заполнен в JSON
- [ ] `bbox_terms` по осям — формулами из параметров.
- [ ] `flex_params` + `nominal_mm` (номиналы = паспортный типоразмер).
- [ ] `connectors` (count/domain/system), `type_name` (номинальный тип), `derived`.
