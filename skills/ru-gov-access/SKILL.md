---
name: ru-gov-access
description: >
  Доступ к российским госреестрам/госсайтам из любого окружения Claude Code, когда
  egress иностранный (системный VPN типа WARP/Дубай, либо форс через иностранный
  корп-прокси) и сайт даёт таймаут/000/заглушку. Блок там ЧИСТО ГЕО — решается
  российским exit-IP. Скилл маршрутизирует запрос: RU-egress → напрямую; иностранный
  → через RU-SOCKS5 (свой надёжный из $RU_PROXY либо авто-источник бесплатных).
  Триггеры: «pub.fsa / Росаккредитация не открывается», «проверь сертификат/декларацию
  в реестре», «ЕГРЮЛ по ИНН / egrul.nalog.ru», «АРШИН / поверка СИ / fgis.gost.ru»,
  «росреестр заглушка/таймаут/000», «нужен российский IP», «VPN мешает росс. сайту»,
  «достучаться до госсайта». НЕ для зарубежных сайтов (там exa/firecrawl/playwright)
  и не для RU-коммерческих (их облако и так берёт — см. [[feedback_web_direct_access]]).
---

# ru-gov-access — доступ к российским госсайтам через RU exit-IP

## Когда нужен
Российские госреестры (`pub.fsa.gov.ru` Росаккредитация, `egrul.nalog.ru` ЕГРЮЛ,
`fgis.gost.ru` АРШИН/Росстандарт, `rosreestr`, `nalog.ru` и т.п.) **блокируют по гео**:
с иностранного IP — таймаут/`000`/заглушка. Проверено 2026-06-22: с российского IP те же
сайты отдают `200` сразу, **антибота на страницах нет — блок чисто географический**.

Кому это надо:
- **ПК физически в РФ** (прокси только в терминале): `curl --noproxy "*"` уже идёт с российского
  IP → госсайты берутся напрямую, скилл не обязателен (но `ru_fetch.py` сам это определит).
- **Машина с иностранным egress** (системный VPN/WARP, либо форс через иностранный прокси на весь
  трафик): прямой запрос и локальный playwright НЕ проходят → нужен RU exit (этот скилл). Egress
  машины ЭФЕМЕРЕН (VPN вкл/выкл) — не привязывать к имени ПК, всегда определять `curl --noproxy ipinfo`.

## Как пользоваться (детерминированный скрипт, без ключей)
```
python ~/.claude/skills/ru-gov-access/tools/ru_fetch.py <URL> [-o out.html|out.pdf]
# опции: -X POST  -H 'Header: V' (повторяемо)  -d '<тело>'  --timeout 20  --tries 16  --no-cache
```
Источники прокси: proxifly (RU pre-filtered) + hookzof (health-check через ipinfo). Рабочий прокси
кешируется на сессию (temp/ru_fetch_proxy.txt) → повторные запросы быстрее; `--no-cache` сбрасывает.

Логика внутри:
1. Определяет РЕАЛЬНОЕ гео egress (`curl --noproxy ipinfo.io` — env-проверки мало, системный VPN в переменных не виден).
2. egress = RU → качает напрямую (`--noproxy`).
3. egress ≠ RU → через RU-SOCKS5: сначала `$RU_PROXY` (если задан), иначе авто-источник —
   тянет свежий список бесплатных RU-прокси (proxifly), health-check каждого через ipinfo
   (country==RU), гонит запрос через первый живой.

Пример (проверено): с egress=AE (Дубай) `ru_fetch.py https://pub.fsa.gov.ru/ -o fsa.html`
→ авто-нашёл живой RU-SOCKS5 → `HTTP 200`, реальный HTML «ФГИС РОСАККРЕДИТАЦИИ».

## Что скилл РЕШАЕТ и чего НЕ решает (честная граница)
✅ **Решает (geo-слой):** открытие госсайтов, статические страницы, **прямые PDF-ссылки**
(паспорта/сертификаты по прямому URL), GET-страницы реестров. Это покрывает основную боль
«сайт/документ не открывается с иностранного IP».

⚠️ **НЕ решает (data-слой SPA-реестров):** JSON-API поиска (FSA `POST /api/v1/rss/common/certificates`,
ЕГРЮЛ-выписка, АРШИН-поиск) возвращает `403` (Content-Length 0, заголовки Spring Security) **на ЛЮБОМ IP** —
проверено 2026-06-22 И с иностранного egress, И с реального офисного RU-IP (AS57712, Мытищи): страница `200`,
а API через curl = `403`. Значит дело **НЕ в гео и не в IP, а в браузерной JS-сессии**: SPA делает handshake
на JavaScript, curl-реплей (даже с cookie+заголовками) его не воспроизводит. Нужен **реальный браузер**:
- **на ПК в РФ** — локальный браузер уже с RU-IP: открыть реестр в Chrome/Edge (для человека) или гонять
  через **playwright** (для Claude) — прокси/сервис НЕ нужны, IP уже российский;
- **с иностранного egress** (VPN/Дубай) — playwright через RU-прокси (живой RU-SOCKS5 из вывода `ru_fetch.py`/`$RU_PROXY`):
  ```
  claude mcp add playwright-ru -- npx @playwright/mcp@0.0.76 --proxy-server=socks5://<RU_IP:PORT>
  ```
  рендерит SPA с RU-IP → данные грузятся. Либо антибот-API с browser+`country=RU`: **ScrapingAnt**
  (10k/мес free) / Bright Data.
- ⛔ **curl-реплей API не работает нигде** (403 даже с RU-IP) — не тратить на это время, сразу браузер.
  ⚠ И синтетический `fetch()` ИЗ браузерной консоли с самодельным телом → тоже **403**: SPA-handshake
  им не воспроизвести. Нужен **реальный UI-ввод/клик «Найти»**, чтобы SPA сам сформировал запрос
  (проверено 2026-06-23 на pub.fsa с локальным RU-IP: клик «Найти» вернул сертификаты, `fetch` с пустым телом — 403).

## Надёжность и безопасность
- Бесплатные прокси **эфемерны** (живут часы/дни) — скрипт берёт свежий список КАЖДЫЙ запуск и
  health-check'ает. Если «no live RU proxy» — повторить запуск или задать `$RU_PROXY`.
- Для СТАБИЛЬНОСТИ и data-слоя — завести надёжный RU-прокси/VPN и положить в окружение:
  `RU_PROXY=socks5h://user:pass@host:port` (per-machine, в `CLAUDE.user.md`/env, НЕ в git).
- **Безопасность:** через случайные бесплатные прокси гонять ТОЛЬКО публичное чтение (открытые
  реестры). Никаких логинов/токенов/ПДн — трафик идёт через чужой сервер. Для авторизованных
  запросов — только доверенный `$RU_PROXY`.

## Раздача через sync-base
Скилл целиком в `~/.claude/skills/ru-gov-access/` (SKILL.md + tools/ru_fetch.py) —
распространяется штатным sync-base, **без ключей и MCP**. Каждый пользователь при желании
добавляет свой `$RU_PROXY`. Опционально (для data-слоя) — добавить ScrapingAnt MCP в
mcp-manifest и личный free-ключ.

## Связи
[[feedback_web_direct_access]] (ШАГ −1, гео-модель 3 состояний), [[web_access_survey_2026_06]]
(полный каталог), CLAUDE.md §«Веб-доступ — лестница» (гео-разрез).
