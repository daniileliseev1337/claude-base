{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://local.invalid/llm-interop/result.schema.json",
  "title": "LLM interop result",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version", "task_id", "status", "summary", "checks", "changes",
    "assumptions", "risks", "questions", "next_step"
  ],
  "properties": {
    "schema_version": {"type": "string", "enum": ["1.0"]},
    "task_id": {"type": "string"},
    "status": {"enum": ["completed", "blocked", "needs_input"]},
    "summary": {"type": "string"},
    "checks": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["name", "status", "evidence"],
        "properties": {
          "name": {"type": "string"},
          "status": {"enum": ["pass", "fail", "not_run"]},
          "evidence": {"type": "string"}
        }
      }
    },
    "changes": {"type": "array", "items": {"type": "string"}},
    "assumptions": {"type": "array", "items": {"type": "string"}},
    "risks": {"type": "array", "items": {"type": "string"}},
    "questions": {"type": "array", "items": {"type": "string"}},
    "next_step": {"type": "string"}
  }
}
