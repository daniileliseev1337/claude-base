---
name: doc-finder
description: |
  Поиск и добыча документа качества (сертификат / паспорт / декларация соответствия /
  техлист) на оборудование по артикулу/бренду. Генерирует точные поисковые запросы и
  добывает официальный PDF (не скриншот) с обходом антибота/прокси.

  Триггеры:
  - «найди сертификат / паспорт / декларацию на <оборудование>»
  - «найди документ качества», «добудь PDF на <артикул/модель>»
  - «нужен паспорт изделия», «скачай сертификат соответствия»
  - «есть ли документ на это оборудование», «где взять РЭ на <модель>»
---

# doc-finder — поиск и добыча документа качества на оборудование

## Когда подключаться
Нужен официальный документ качества (для ИД, проверки поставщика, спецификации) по
артикулу/бренду оборудования. НЕ для произвольного веб-поиска — именно документы качества.

## Workflow

### Шаг 1 — Сгенерировать поисковые запросы (СКРИПТ)
```bash
python ~/.claude/skills/doc-finder/tools/gen_dorks.py --article "<артикул>" --brand "<бренд>" --type сертификат [--site <домен>]
```
`--type`: `сертификат` / `паспорт` / `декларация` / `любой`. Скрипт строит Google-dork
запросы (filetype:pdf, intitle:"index of", site:). Сети не требует.

### Шаг 2 — Выполнить запросы через web
Прогнать каждый запрос через web-лестницу (канон — [[feedback_web_direct_access]] / CLAUDE.md
§«Веб-доступ — лестница»). Цель — карточка товара актуального артикула на сайте производителя/B2B-поставщика.

### Шаг 3 — Добыть официальный PDF
По методам [[feedback_web_direct_access]]:
- зайти в карточку товара → раздел «Документация / Сертификаты», брать ссылку ОТТУДА
  (не конструировать URL, не из Google — часто битый);
- за антиботом/сессией: playwright cookies → `curl --noproxy "*" -b cookies`;
- проверить сигнатуру `head -c4`=`%PDF` (не заглушка-HTML);
- последний код артикула: полный комплект на странице актуального кода (Метод 4).

**Гео/egress-модель** (RU vs иностранный egress, госсайты) — канон [[feedback_web_direct_access]] ШАГ −1.
Для добычи кратко: иностранный egress → росс. сайты только облаком; росс. ГОССАЙТЫ (pub.fsa/ЕГРЮЛ/АРШИН) → скилл [[ru-gov-access]].

### Шаг 4 — Верификация добытого
- Открыть/рендерить 1-ю страницу — это нужный документ на нужный артикул (не заглушка).
- При сомнении в подлинности — [[supplier-due-diligence]] `verify_pdf_metadata.py`.
- **Verify:** PDF официальный, на нужный артикул, читается. «Документа нет» — только после
  визуального просмотра правильной карточки на правильном домене.

## Tools (слой 3)
- `tools/gen_dorks.py` — генератор поисковых запросов (offline). Протестирован.

## Связь с базой
- Использует [[feedback_web_direct_access]] (обход прокси/антибота, curl cookies) как методы добычи.
- Питает `снабженец` и `id-engineer` (документы качества для ИД).
- Подлинность добытого — [[supplier-due-diligence]].

## Ловушки
- Скриншот документа ≠ документ качества — для ИД нужен официальный PDF.
- Прямая ссылка из Google/exa часто битая — брать из карточки товара (Метод 3).
- Госреестры (pub.fsa) при иностранном egress (системный VPN) дают таймаут/заглушку — их
  страницы/PDF брать через скилл [[ru-gov-access]] (RU-IP), либо искать документ на B2B/у производителя.
- **Обрыв TCP/таймаут КОММЕРЧЕСКОГО сайта с иностранного egress = геоблок, а НЕ «документа нет»
  и НЕ повод откладывать добычу на другую машину.** Три обязательные ступени дожима (боевой
  случай 2026-07-07, отказное Geberit со smartcomplect.com): (1) **зеркала** — искать точное
  имя файла / № письма у других дилеров (геоблок у каждого свой; santehgrad отдал напрямую то,
  что smartcomplect резал); (2) **RU-exit для прямых PDF**:
  `python ~/.claude/skills/ru-gov-access/tools/ru_fetch.py <URL> -o f.pdf` — работает и на
  коммерческих сайтах с геоблоком, не только на госреестрах; (3) **Wayback Machine**
  (web.archive.org) — отдаёт исходные байты PDF с любого egress. «Не качается с этой машины» —
  вывод только после всех трёх ступеней. Субагенту-добытчику прописывать эти ступени в промпт явно.
