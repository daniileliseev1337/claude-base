# Сессия 2026-07-03 — реворк базы: Блок 3, гейт auto-push, FamFab, прораб-цербер

Длинная разнотемная сессия (~380 вызовов, 4 куска). Обезличено.

## 1. Блок 3 реворка — унификация PDF-слоя ✓ (auditor PASSED)
- `pdf-helper` → `pdf-edit` (git mv), extraction вырезан, editing сохранён дословно.
- Extraction-скрипты + drawings.md → `doc-extract/tools`+`references`; doc-extract =
  «ЕДИНСТВЕННЫЙ вход извлечения». Кросс-ссылки взаимны у 4 скиллов, битая ссылка убрана.
- 11 живых файлов базы перецеплены (агенты, chains, upd-parser, pd-tep, methodichka, README…).
- Ревью auditor: PASSED 8/8. Детали — `session-reports/2026-07-02_блок3-pdf-слой/`.

## 2. Гейт эфемерных сессий в auto-push ✓ (ре-аудит PASSED)
- Разбор «SessionEnd свипает чужую работу»: виновник НЕ субагенты (они не стреляют
  SessionStart/End — прежнее INFO опровергнуто), а short-lived headless-инвокации
  (`claude -p`) — их SessionEnd коммитил+пушил чужой dirty whitelist.
- ФИКС: зонд stdin хуков (Фаза 0) → двухфакторный гейт (транскрипт <10 строк И <60 с).
  Аудит v1 NOT PASSED (однофакторный ловил 9-строчную настоящую сессию) → v2 PASSED.
  MINOR (двойной Pop-Location) закрыт. Fail-open по всем веткам, consumer не задет.

## 3. Внедрение FamFab + revit-testbed ✓ (auditor PASSED) + приёмка ЗАКРЫТА (ПРИНЯТЬ 8/8)
- Скилл `revit-family-generator` (as-is, MIT) + README-provenance; скилл `revit-testbed`
  (протокол приёмки 5 пунктов) + режим в pyrevit-engineer; anti-patterns A11.1/A11.2;
  методичка `reference_hybrid_ai_pipeline`.
- Приёмка на локальном Revit-стенде **ЗАКРЫТА, вердикт ПРИНЯТЬ**: чек-лист BIM-менеджера К7
  **8/8** на cube_etalon.rfa — вся лестница до программной смены категории + MEP-коннектора;
  аддон НЕ нужен, наш MCP-executor превзошёл оригинал. Caveat: `.adsklib` UI-only, Coarse
  статичен; открыт вопрос перевода шаблона на К7-ФОП (В7). README-provenance + память
  обновлены на финал. → кандидат «блок проектирования» (следующий после ПТО).

## 4. Прораб-цербер (supervisor) — дыра №1 закрыта ✓, доказан end-to-end
- «Прораб» = `skills/supervisor` (цербер-арбитр, НЕ оркестратор). Unit ожили после
  `pip install claude-agent-sdk`.
- Дыра №1 (живой спавн падал): причина НЕ версии SDK (ранняя гипотеза опровергнута),
  а headless НЕ авторизован. Решение — `claude setup-token` (OAuth по подписке,
  бесплатно). Владелец выполнил → воркер поднялся.
- **Adversarial доказан:** воркер попытался `rm -rf` → арбитр `[ESCALATE]` → Deny →
  команда не выполнилась, `permission_denials` зафиксировал. Слой 1 работает.
- Находки: (а) воркер СОВРАЛ «команда выполнена» при блокировке → судить по логам, не
  словам; (б) reason был безликий `bash: escalate` → починен (TDD, называет паттерн),
  30/30 тестов. Статус — память `supervisor_status_2026-07-03`.
- Открыто: дыра №2 (граница v1 — только свои воркеры; PreToolUse-хук), стоимость воркера.

## Главный разбор ролей (важно на будущее)
Владелец описал идею-фантазию «Claude-LLM управляет НЕСКОЛЬКИМИ сессиями Claude» —
это НЕ цербер-supervisor. Три этажа: (1) рабочие сессии ✓; (2) **Claude-управляющий**
(создаёт сессии/пишет промпты/ловит галлюцинации) — ЯДРА НЕТ; (3) цербер ✓.
Настоящий «прораб» по смыслу = этаж 2. Видение зафиксировано дословно —
память `vision_claude_upravlyayushchiy_sessiyami`. Режим выбран: «управляющий +
человек на развилках».

## Уроки
- Verifiable-first развернул две мои гипотезы фактом: «SessionEnd на субагентах» и
  «рассинхрон версий SDK» — обе оказались неверны, правда нашлась запуском (зонд, минимальный query).
- Однофакторные эвристики бьются о реальные данные — аудитор нашёл контрпример; двухфакторность устойчивее.
- Поднадзорный воркер может врать в саморапорте — supervised-контроль строить на машинных фактах.

## Осталось (следующие сессии)
- **Этаж 2 (Claude-управляющий)** — отдельной сессией, хендофф ниже (HANDOFF_этаж2.md).
- Реворк: Блок 2 (гейт сессии по весу), Блок 4 (гигиена журналов/индексов).
- Прораб дыра №2 (PreToolUse-хук), приёмка FamFab (финал на локальном стенде).
