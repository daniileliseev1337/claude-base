# Сессия 2026-07-09 — web-access, project-memory v2, пилот deep-research (DANIIL-LAPTOP)

Старт: разбор feedback-inbox (реворк базы). Дрейф по живым болям → 4 крупных работы.

## Сделано

### 1. auto-push оживлён (критический фикс инфры)
Симптом (вопрос владельца «почему опять не работает»): с 02.07 гейт эфемерных сессий
скипал ВСЕ настоящие SessionEnd → 5 коммитов копились локально. Корень (зонд
hook-probe.jsonl): stdin хука читался в OEM, кириллица в transcript_path ломалась,
Test-Path не находил файл → «нет файла = эфемерная». Фикс `3d3091c`: UTF-8 InputEncoding +
срез BOM в auto-push И auto-pull; отсутствующий транскрипт → fail-open. Урок 17 в
`memory/2026-05-09_hooks-debugging.md` (3-я кодировочная ловушка хуков за месяц). Бэклог
06-07.07 (toolgate v2.5, project-memory хуки, гигиена) доехал команде.

### 2. Скилл web-access (унификатор веб-доступа)
Боль: лестница жила текстом, модель перебирала инструменты по памяти, «в интернете нет»
после 2 неудач. Построен `skills/web-access/tools/web_get.py` — детерминированная лестница
в коде (direct→noproxy→r.jina→RU-exit), верификация (не заглушка/%PDF/challenge), next_hint
на MCP-остаток. Боевые фиксы на реальных URL (palerom/tincraft, зонты владельца):
- **кодировка**: падал «на выводе» — UnicodeEncodeError cp1251 (тестировал с
  PYTHONIOENCODING, не в среде пользователя — промах). force_utf8_stdio() + render_human().
- **маршрутизация RU-host**: медленный ru_fetch стоял первым — palerom берётся прямым curl.
  Убрал ru из авто-лестницы в next_hint; noproxy-detect (WARP: --noproxy мёртв).
- **egress-кеш** на сессию: 40с→5с (замер lunda.ru).
- **анти-капитуляция + реестр главных сайтов** (`tools/main_sites.md`, приоритет по ТИПУ
  источника — производитель→агрегаторы→реестры, не полный список). Правило в CLAUDE.md.
37 юнит-тестов. Коммиты 303ddf4→e712bda.

### 3. Пилот deep-research (вердикт: редкий инструмент)
2 прогона (рекуператоры + VRF-рынок) — БОЕВОЙ замер: 71+103 субагента, ~$162/сессия,
cache-read 43M, 5ч-лимит до 89%, оба УМЕРЛИ в фазе verify → 0 отчётов. Дорого И хрупко.
Зафиксировано в `token_economy.md`: тяжёлый fan-out — ТОЛЬКО по явному запросу с ценником
ДО старта. Урок процесса: «2 теста» без ценника = ~$162 зря.

### 4. project-memory v2 Этап 1 (спека→план→реализация)
brainstorming → спека + план (`docs/superpowers/{specs,plans}/2026-07-09-project-memory-v2*`).
Лечит боль «не держит контекст» (cwd непредсказуем). Реализовано (Task 1-5, субагенты sonnet
+ инлайн): КОНТЕКСТ.md + domain_to_agent (мостик к 16 агентам); find_project.ps1 (walk-up);
project_context.ps1 (доставка path-канал); гейт мутаций (log-tool-usage регистрирует Read →
project_gate.ps1 exit 2). **auditor NOT PASSED → 2 BLOCKER (журнал не доставлялся; латентность
600мс) + MAJOR (коллизии domain_to_agent) ИСПРАВЛЕНЫ**, 70 тестов, вес-гейт 27/27, латентность
600→285мс. **Локальная активация** (settings.local.json — не уедет команде, не затрётся
merge-shared) для обкатки. Коммиты 56d48f0→8f00bda. Память: [[project_memory_skill_done]].

### 5. Боль агентов — оформлена в бэклог
Владелец: ревьюеры спотыкаются «нет доступа к инструментам» (~6/8 ложных). Факты собраны,
диагноз-затравка (холодный office-MCP / нет Bash-fallback) → [[agents_audit_backlog]],
фокус-сессия.

## Где сломался / уроки
- Кодировка на границе процесса — ТРЕТИЙ раз за день (auto-push stdin, web_get вывод, +
  проверял чужие). Правило: любой хук/скрипт с stdin/выводом — UTF-8-обвязка первой строкой,
  тест-данные с кириллицей в путях.
- Тестировал web_get с PYTHONIOENCODING=utf-8 → не поймал cp1251-баг среды пользователя.
  Verifiable-first: тестировать в среде пользователя, не тепличной.
- auditor окупился: нашёл 2 BLOCKER, которые тесты субагентов не ловили (журнал, латентность).
- Первый smoke гейта дал ложный exit 0 (мой невалидный JSON) — verifiable-first спас от
  ложного вывода «баг».

## Открыто (следующие сессии)
- project-memory этапы 2+ (⑥ живой дашборд СТАТУС.html, ⑤ реакция на жалобы, ④ опц. файлы).
- Обкатка project-memory v2 локально → раздать команде (settings.shared).
- **Боль агентов** ([[agents_audit_backlog]]) — фокус-сессия аудита 16 агентов.
- **Таблица кандидатов 1-10** (feedback-inbox) — изначальная задача, НЕ разобрана.
- Diff CLAUDE.md применён ранее; ИНН/ФИО — оставлены (решение владельца).
