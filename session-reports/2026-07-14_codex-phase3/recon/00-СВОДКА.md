# Сводка веера разведки — Фаза 3 мульти-LLM (2026-07-14)

5 субагентов sonnet, параллельно. Детали — файлы 01–05 рядом.

## Топ-находки, меняющие планы эпиков

1. **[Эпик 3] Корень падения Revit/AutoCAD под Codex — НЕ кириллица, а CRLF-баг Python MCP SDK на Windows** (stdio.py шлёт `\r\n`, Rust-клиент Codex строг, TS-клиент Claude терпим). Плюс отдельный класс: десктоп-Codex на Windows валится целиком от сбоя одного MCP вопреки `required=false` (открытые #16834/#29321). Фиксы: локальный патч `newline=""`, `required=false`+`startup_timeout_sec`, ASCII-обёртка как страховка, мост по требованию. Lazy-MCP как фичи не существует.
2. **[Эпик 2] `[profiles.X]` в config.toml deprecated с 0.134** — профили теперь отдельные файлы `~/.codex/<name>.config.toml` + `codex --profile`. Эталон проектировать под это; генератору нужен рендер-шаг профилей. Документация переехала на learn.chatgpt.com/docs/config-file/* (истина — config-sample).
3. **[Эпик 2] VS Code почти бесплатен:** плоский AGENTS.md в корне проекта покрывает разом Copilot agent mode (`chat.useAgentsMdFile` default TRUE), Cursor (нативно), Codex-extension (через ~/.codex), Gemini CLI (`context.fileName`). Ловушка: `chat.useClaudeMdFile` НЕ разворачивает `@import` — канал CLAUDE.md мёртв без плоской генерации. Continue.dev AGENTS.md не читает. VS Code MCP: `mcp.json` ~1:1 наш формат + автообнаружение из Claude Desktop (проверить!).
4. **[Эпик 1] Наш hooks.json официально валиден** (формат совпадает, фича hooks GA/stable/true), но не исполняется: либо не прошёл обязательный trust review, либо десктоп-регрессия #21639 (открыта). Вывод: автосинк строить на **хуке Claude Code** (основной) + FileSystemWatcher (страховка); хуки Codex — не единственный канал. Дрейф — mtime/hash-скан, API не нужен.
5. **[Эпик 4] Оркестрация готова с двух сторон:** `codex exec` (--json, -o, --output-schema, resume, работает под ChatGPT-логином; бинарник НЕ в PATH — полный путь `%LOCALAPPDATA%\OpenAI\Codex\bin\<hash>\codex.exe`) и `claude -p`/`claude mcp serve`. Существует **официальный плагин openai/codex-plugin-cc** (28.5k★): /codex:review, adversarial-review, transfer, --background. Кросс-вендорной эстафеты сессий из коробки нет → markdown-handoff-пакет (наш скилл) — правильный путь. Кодировка: форсировать UTF-8 (подтверждённая ловушка PowerShell).

## Что проверить живьём на тест-заезде (Эпик 5)
- Текст ошибки MCP-рукопожатия в логе Codex (`connection closed: initialize response` = CRLF подтверждён).
- Trust review хуков + исполняются ли hooks в `codex exec` (другой code-path, не GUI).
- Автообнаружение VS Code MCP из Claude Desktop; `.agent.md`/`.claude/agents` в живом VS Code.
- Квота `codex exec` (жжёт ли общий пул — гипотеза).
- tools `codex`/`codex-reply` в `codex mcp-server` (live-дамп параметров).
