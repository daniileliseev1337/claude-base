# Сессия 2 (2026-06-23): детальная перепроверка 12 ВОР + координатный метод + фикс 5.6

> Обезличено: объект → «<объект, больница, ЛНР>»; ФИО → роли (составил/проверил/н.контроль/ГИП).
> Технические шифры (ПР-01-ИОС5.X.СО), номера позиций, страницы — сохранены для продолжения.

## TL;DR
- Триггер: пользователь сказал «доверия нет» — при проверке прошлой (перегруженной) сессии на первом ВОР нашлась ошибка. Оказалось: **ошибка была не в файлах, а в ЛОЖНОМ УТВЕРЖДЕНИИ прошлой сессии**, что «во всех штампах Балашиха». Перепроверка показала обратное — во всех 12 штампах объект = Северодонецк.
- Закрыт незакрытый вопрос «рендер vs текст»: **координатная экстракция таблиц СО** (`PyMuPDF page.find_tables(strategy="lines")`) — детерминированно, читает реальный text-layer, дешевле и точнее визуального рендера. Сделана основным методом.
- Построен переиспользуемый детерминированный сверщик; найдено и подтверждено **9 реальных ошибок завышения кол-ва в ИОС5.6** (patch cord / SFP+: было 4, в СО = 2). Исправлено, пересобрано, отревьюено (excel-validator + auditor на opus — оба PASS).
- Остальные 11 разделов по количествам/единицам/позициям/штампу/мете — **чисто**.

## Метод (3 слоя, все детерминированные)
1. **Экстрактор СО из PDF** — `verify_vor.py: extract_co()`. `find_tables(strategy="lines")` строит сетку по векторным линиям таблицы → каждое слово в правильную ячейку (решает проблему «сырой текст путает колонки», из-за которой прошлая сессия ушла в рендер). Учтены ловушки: страницы СО **повёрнуты на 90°** (rotation=90, координаты в неповёрнутом пространстве — find_tables справляется сам); заголовки колонок с **переносами** («Коли-\nчество», «Единица измере-\nния») — дегифенизация; разные шапки («Поз.» vs «Позиция»); **название в данных уезжает на 1 колонку** относительно шапки → название = склейка ячеек от позиции до тип/код; **безномерные строки** в PDF (напр. Pigtail с пустым номером) — берём по наличию кол-ва.
2. **Сверщик СО(PDF)↔json** — гибрид: матч по (СО,позиция) + добор по нормализованному названию (нормализация гомоглифов лат/кир: `4x3`↔`4х3`, `M8`↔`М8`, `c`↔`с`). Снимает ложные тревоги от разных схем нумерации и опечаток.
3. **Генератор xlsx==json** — `check_xlsx.py`. Подтвердил: генератор переносит данные 1:1 (0 расхождений по всем 12).

**Ключевая дисциплина:** детерминированный прогон сужает 546 строк до горстки расхождений → каждое подтверждается визуально по конкретной странице PDF. Где экстрактор и json совпали — оба верны; где разошлись — смотрим источник. Так пойман и системный недосмотр, и не наплодили ложных срабатываний.

## Найдено и исправлено (ИОС5.6)
9 позиций, кол-во 4 → 2 (patch cord F/O LC-LC и SFP+ 10G модули):
СО2-5.11, СО2-5.24, СО2-6.11, СО2-6.24, СО2-7.11, СО2-7.24, СО3-3.24, СО3-4.11, СО3-4.24 (страницы PDF 67-77).
Прошлая сессия нашла ОДИН такой (СО3-3.11) и починила, 9 однотипных рядом — пропустила (вижн-чтение 546 строк не масштабируется). Правка — `_fix56.py` (хирургический регэксп в пределах объекта, с проверкой что было 4); пересборка `build_from_json.py`. Верификация: `verify_vor 6` = 0, `check_xlsx 5.6` = 0, fullCalcOnLoad=True.

## Ловушки-уроки
- **Опечатка в самом PDF:** в СО3 узле 3 патч-корд напечатан с номером «2.11» вместо «3.11» (стр.75) — экстрактор честно прочитал кривой ярлык, дал ложный DUP/EXTRA. json разнёс верно. Вывод: номер позиции в PDF ненадёжен (опечатки, пустые, мульти-СО) — матчить гибридно (позиция+название), не только по номеру.
- **Глаз ошибается, text-layer — нет:** при визуальной сверке я один раз прочитал «16»/«25» там, где text-layer (find_tables) и оба ревьюера дают «10»/«20». Для ВЕКТОРНОГО PDF извлечение из text-layer авторитетнее визуального чтения рендера (рендер — лишь растеризация того же слоя; ошибается человек). Рендер нужен только для адъюдикации единичных спорных ячеек.
- **Виды работ keyword-правилом не проверяются детерминированно** — слова «винт/кабель/труба» внутри описаний оборудования дают массу ложных флагов. Нужен семантический ревьюер, если потребуется.
- **5.12 нюанс (НЕ трогали):** наш ВОР = «Система **вызывной** палатной сигнализации» (совпадает с папкой СВПС), штамп исходного PDF = «Система палатной сигнализации» (без «вызывной»). Источник сам себе противоречит — оставлено на решение пользователя.

## Переиспользуемые активы (в папке проекта)
- `verify_vor.py` — экстрактор СО + гибридный сверщик кол-ва/ед/позиций (`python verify_vor.py [N]`).
- `check_xlsx.py` — сверка генератора xlsx==json по всем разделам.
- `check_extended.py` — наименования (токенный diff) + виды работ (keyword-правила).
- Кандидат в скилл: «координатная верификация ВОР↔СО» (детерминированная альтернатива вижн-аудиту крупных спецификаций). Эвристика CLAUDE.md «deterministic-first перед Workflow» сработала образцово на матчинге 546 строк.

## Артефакты
- Исправлен: `ВОР_ИЗМ\ВОР-ИОС5.6.СО.xlsx` (+ `_data_ИОС5.6_изм.json`). Остальные 11 — без изменений (верны).
- Ревью: excel-validator (форма, opus) PASS; auditor (содержание vs СО, opus) PASSED.

## Доп. деливераблы этой сессии
1. **Word-отчёт изменений ПД 06.22→06.23** (`Изменения ПД ИОС5_06.22-06.23.docx`): детерминированный
   diff СО двух редакций (`co_diff.py`, по последовательности названий). Итог: содержательно изменились
   2 раздела — **ИОС5.9** (Переговорное устройство 7→8, Модуль грозозащиты 9→8) и **ИОС5.11** (замена
   часофикации НЕТСИЛОН/ТРЕНД/СТРИМ → КОНТУР, 10→9 позиций); остальные 10 — СО без изменений. Объект в
   штампе исправлен **Балашиха→Северодонецк в 11 разделах** (старая ред. содержала ошибочный адрес;
   5.12 уже был Северодонецк). Развенчан handoff-артефакт «5.2 NVR 3→4» — ПД обеих редакций содержит 4
   (3+1), это была ошибка транскрипции старого ВОР, не правка ПД.
2. **Природа исходной претензии («доверия нет»):** прошлая сессия УТВЕРЖДАЛА «во всех штампах Балашиха»
   применительно к НОВЫМ файлам — а в новой ред. везде Северодонецк (ложная тревога). Балашиха была в
   СТАРОЙ редакции. Урок: утверждения о состоянии файлов проверять детерминированно, не по памяти.
3. **Новый скилл `~/.claude/skills/co-verify/`** (hub, 3 слоя): `tools/co_engine.py` (движок, project-agnostic),
   `co_verify.py` (сверка ВОР↔СО из json/xlsx), `co_diff.py` (diff редакций), `examples/case-vor-co.md`.
   Протестирован на файлах проекта (co_verify 5.6 = 0, co_diff 5.9 = 2 изм.). Граф базы после добавления
   скилла+памяти — пересобрать `/graphify ~/.claude --update`.
