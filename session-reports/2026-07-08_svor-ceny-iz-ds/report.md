# Сессия 2026-07-08 — внесение цен из ДС (docx) в СВОР (xlsx), раздел «мультимедиа»

## Задача
В комплект из 8 СВОР (сопоставительные ведомости, 12 граф A–L) внести цены из 8 спецификаций
ДС (docx-таблицы «№|Производитель|Модель|Описание|Ед.|Кол-во|Цена с НДС|Стоимость с НДС»):
две новые колонки M/N («Цена за ед.» гр.13 / «Стоимость» гр.14), цены ТОЛЬКО на строки
оборудования/материалов (работы без цен), N = живая формула =ROUND(M*G,2) от объёма «с учётом
изменений», строка «ИТОГО за раздел» перед подписями, выход в отдельную папку. Пилот — пара
«раздел ММ ↔ СВОР МС», ядро переиспользуется на остальные 7 пар.

## Что сделано
- Грилинг: 5 решений пользователя зафиксированы в FACTS.md проекта (формула, расхождения
  объёмов → в отчёт, ПНР из ДС в СВОР НЕ вносится — пользователь сначала выбрал «внести
  позицией», затем сам отменил; оформление/папка).
- Ядро: `tools/svor_pricing.py` (parse-ds → parse-svor → match → apply → verify, CLI по slug),
  FACTS.md, work/<slug>/ с JSON и отчётами, dashboard.html (вкладки по разделам, 4 уровня
  важности находок).
- Пилот пройден: 460/466 позиций ДС получили цены (суммы обезличены — см. FACTS.md проекта),
  ИТОГО за раздел сошлось с расчётом двумя независимыми путями (Python Decimal и живой Excel),
  финансовое тождество «ИТОГО + ПНР + недобор объёмов + непопавшие позиции = итог ДС» —
  копейка в копейку.
  Верификация 15/15 PASS. Ревью: excel-validator WARN (только ограничения его тулинга,
  0 дефектов; все его «непроверенные» пункты закрыты детерминированной verify-стадией),
  auditor — см. дополнение ниже.

## Ключевые уроки (переиспользуемые)
1. **Матчинг ДС↔СВОР — по описанию, № не ключ.** Нумерация расходится (в ДС дыра в 34 номера
   без позиций — вычеркнули без перенумерации; сумма при этом сходится с итогом).
   Алгоритм: монотонный указатель + ОКНО (40 строк пула), fallback «производитель+модель»
   в окне, затем строгий fuzzy (ratio≥0.95 + ед.изм + кол-во), глобальный добор только по
   точному описанию;模ель глобально — только при единственном кандидате.
2. **Глобальный прыжок указателя = лавина**: одна нематчащаяся позиция (описание в СВОР
   отредактировано: другая ревизия железа) утаскивает указатель к дублю в другом разделе,
   и весь остаток файла матчится «вне порядка». Окно вместо глобального скана лечит.
3. **Model-fallback ворует строки РАБОТ**: строки-работы наследуют производителя/модель через
   вертикальный merge блока → ключ (производитель, модель) у работы совпадает с оборудованием,
   а работа стоит ВЫШЕ. Фильтровать кандидатов по глагольным префиксам работ.
4. **Гомоглифы**: кириллические а/е/о/р/с/у/х/к в латинских моделях и наоборот — фолдить с
   ОБЕИХ сторон (translate-таблица после lower()); но тогда и ВСЕ сравнения с русскими
   литералами («Раздел», «Составил», «Итоговая сумма») обязаны идти через ту же нормализацию —
   иначе тихо ломается детект секций/подписей/итога.
5. **Дубли описаний с разными ценами в ДС** (8 шт: мониторы, камеры, кабели по залам) —
   именно поэтому порядок следования обязателен в ключе матчинга.
6. **Одна строка СВОР на две позиции ДС** (та же опция в двух залах, в СВОР учтена одна):
   монотонность отдаёт строку «своему» залу, второй экземпляр честно попадает в непокрытые.
7. **Ручные привязки**: manual_overrides.json (ds_num→строка) — механизм нужен, но овверрайд
   ОБЯЗАН проверять, что строка не принадлежит другой позиции по точному совпадению
   (мой первый овверрайд отнял строку у законного владельца — отменён).
8. **win32com, русская локаль**: `Range.NumberFormat = "#,##0.00"` кидает COM-ошибку —
   падать в `NumberFormatLocal = "# ##0,00"`. Вставка строк через `Rows.Insert` — формулы/merge
   сдвигаются корректно; verify обязан сравнивать merge с учётом сдвига (+2 ниже точки вставки).
9. **Запись колонок в конец (M/N) через COM безопасна**: не трогает формулы граф 1–12
   (self-referencing IF/ROUND) — подтверждено сравнением с эталоном (счётчик формул + выборка).
10. **docx-числа**: «Кол-во» в ДС бывает «шт» при фактических метрах — сверять ЧИСЛА
    (кол-во ДС == объём СВОР), а не доверять метке единицы; безобидно, если числа равны.
11. **excel-validator без Bash**: в сессии субагента может не оказаться Bash и кэша значений
    (read_data_from_excel отдаёт формулы) — планировать так, чтобы детерминированная verify-стадия
    оркестратора закрывала кэш/ширины/merge-diff заранее, а ревьюеру оставлять форму и сравнение
    с эталоном.

## Где споткнулся
- Дважды тихо ломал собственные сравнения литералов после введения гомоглиф-фолда (уроки 4).
- Задал пользователю вопрос про спорную привязку на неполных данных (не проверил, что строка
  уже занята точным матчем) — пришлось отменять решение. Правило: перед вопросом о привязке
  проверять владельца строки.
- PowerShell 5.1: инлайн `python -c` с `[:75]` и кавычками ломается парсером — временные
  py-файлы в scratchpad надёжнее.

## Продолжение сессии — батч 7 оставшихся разделов (та же дата, новый чат)

- Прогнаны конвейером все 7 оставшихся пар (АХП, СКС, СОТ, СТМ, СКПТ, СВМ, ЭЧ): parse → match
  (batch-скрипт `run_batch.py` с glob-маппингом пар) → глубокий разбор отчётов матчинга →
  пакет спорных вопросов пользователю → manual_overrides → apply/verify/recount
  (batch-скрипт `run_apply.py`, COM последовательно) → ревью-волны excel-validator ×7 +
  auditor ×7 (sonnet, параллельно в фоне).
- Итог: все 8 разделов ГОТОВЫ; verify 15/15 и recount 0 нарушений на каждом; финансовые
  тождества с итогами ДС сходятся копейка в копейку (суммы — в FACTS.md проекта).
  Ревью: 7×PASS (validator) + 7×PASSED (auditor).
- Ручных привязок: 31 на 4 раздела; 2 пары «предположительные» подтверждены фактом матчинга
  (95%+). Пользователь по ходу правил ИСХОДНЫЙ СВОР одного раздела (объёмы двух строк) —
  потребовался перепарсинг; одна правка исходника сделана мной win32com по его поручению.

## Уроки батча (переиспользуемые, дополняют уроки пилота)

12. **Δобъёмов в verify считать по `total_ДС`, не `price×qty_ДС`**: в ДС встречается
    округлённое «Кол-во» при «Стоимости», посчитанной от дробного объёма == G СВОР
    (крепёж), и усечённые до 1 знака стоимости (итог ДС внутренне расходится на копейки) —
    verify должен принимать recon == Σ позиций ДС + ПНР как валидный исход с пометкой.
13. **Дубли-пары с разными объёмами (лицензии на 49 и 13 АРМ) матчер крестит**: правильная
    привязка определяется qty==G и разными ценами партий; лечится overrides, сначала
    проверив владельцев строк. Аналогичный swap был и у «опций поддержки» рядом.
14. **СВОР без блока подписей**: строку ИТОГО ставить через пустую строку после данных,
    чек подписей в verify делать условным.
15. **Агрегация СВОР (G=Σ количеств двух позиций ДС при одной цене) — self-consistent**:
    деньги ложатся полностью через M×G, вторая позиция честно висит в «непопавших», Δ-член
    формулы компенсирует — тождество сходится без спецобработки.
16. **Ложная находка ревьюера (потеря merge)**: ревьюер вручную сканировал длинный
    однострочный JSON merged_cells и «не увидел» диапазоны. Первый шаг при расхождении —
    свой детерминированный openpyxl-diff обоих файлов, затем SendMessage с фактом; ревьюер
    сам снял WARN. Резолвер-синтезатор не понадобился (расхождение инструментальное).
17. **Пакет вопросов пользователю с рекомендациями окупается**: 4 вопроса с проверенными
    владельцами строк и денежными последствиями в описании опций — все 4 приняты по
    рекомендации; одна правка вводных (G строки в исходнике) всплыла только благодаря
    вопросу с суммой ×5 в тексте опции.
18. **Числовое доказательство вместо вопроса**: привязки с точным совпадением объёмов
    (18=18, 99=99, 1076=1076, серия 7/7/7/8/8/15) и сходящимися деньгами применять
    overrides'ами без вопросов, вопросы — только на методологию (куда класть цену сборки)
    и на реальные расхождения данных.

## Довесок: рыночная оценка позиций вне ДС (копия папки, +30%)

- По запросу пользователя создана полная копия готовых файлов; в ней довнесены цены на
  11 строк СВОР с объёмом, отсутствующих в ДС (лючки/розетки ДКС, АКБ и ИБП Бастион,
  кронштейн Dahua, огнестойкая пена). Рыночные цены собрал агент-снабженец (sonnet,
  2 источника на позицию, медианная розница с НДС), наценка +30% к цене за единицу,
  запись `tools/apply_market.py` (win32com, guard по описанию строки + запрет перезаписи
  занятых M). «Алмазное бурение» (работы) — пользователь исключил явным решением.
- Находка снабженца: артикул кронштейна Dahua из СВОР в каталоге производителя не
  существует — взята цена точного аналога по описанию с пометкой, эскалировано.
- Ревью дельты: auditor PASSED (полный diff столбца M: ровно 11 изменений; проверка
  round-half-up по диагностическому случаю), excel-validator PASS (его методический WARN
  по merge закрыт моим openpyxl-diff — снова урок №16).
- Уроки: (а) точечное довнесение в ГОТОВЫЕ артефакты — строки данных не сдвинуты, номера
  строк исходника валидны, формула ИТОГО подхватывает новые N сама; (б) ревью дельты
  дешевле полного: по одному агенту каждого типа на пакет изменённых файлов с точным
  списком правок в промпте; (в) снабженцу запрещать самому делать наценку — проценты
  считает оркестратор Decimal'ом.

## Дополнение (auditor)
- Первый вердикт NOT PASSED с единственным BLOCKER-METHOD: аудитор не мог сам пересчитать
  итог (его excel-MCP отдаёт текст формул вместо кэша, Bash в его сессии не выдан).
- Закрыто предписанным им способом: независимый построчный пересчёт по кэшу артефакта
  (recount_n.py, openpyxl data_only=True) — 460 строк, 0 нарушений, Σ == кэш итога;
  скрипт и результат отправлены аудитору через SendMessage (продолжение его контекста).
- Финальный вердикт: **PASSED, 9/9 свойств** (код скрипта отревьюирован им независимо +
  спот-чек). Урок: ревью-гейт для xlsx планировать так, чтобы кэш-зависимые проверки
  закрывались детерминированной стадией оркестратора, а аудитору отдавать код+результат
  на код-ревью — работает как «расхождение ревьюеров» без спавна резолвера, когда
  расхождение чисто инструментальное.
