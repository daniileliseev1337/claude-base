# Сессия: валидация личной ПТО-базы на открытых вопросах vault

**Дата:** 2026-06-20. **Модель:** Opus 4.8. **Канал vault:** MCP Obsidian.

Продолжение сессии «открытые вопросы ПТО» (см. handoff `2026-06-20_pto-open-questions-daniil`).
Цель: проверить, верно ли личная ПТО-база (доменный агент `id-engineer` + граф/методичка ИД +
реальные тома объекта) отвечает на доменные вопросы из vault. Совпадение с практикой
customer zero = и ответ на вопрос, и валидация базы.

## Что сделано

Обработаны 3 целевых вопроса (flow: база отвечает → практик подтверждает/правит → запись в ноту):

- **SD-116** (перенумерация выпущенного акта) — закрыт. Q1 сценарии (вставка пропущенного,
  исправление ошибки); Q2 порядковый + шифр раздела; Q3 только каскад; Q4 подписанный заморожен
  по номеру, кроме переформирования ИД с переподписанием.
- **SD-100** (Том ИД) — закрыт целиком (все Q — зона Даниила). Q3 мандаторность ВОР/ВСО
  (таблица по типам актов + warning, дифференцированный по типу акта); Q1 без оценки страниц в
  пилоте; Q2 копия на период (наименование+состав+флаги, период автосдвигом, новый Draft +
  сохранение прошлой собранной версии).
- **SD-102** (раздел РД: проектная докум. + добивки) — закрыт. Стадия П как `kind: project`;
  отдельный ordinal «Поз.» для движка было-стало; датированная редакция; экспорт+импорт
  спецификации в Excel на бэке.

По запросу практика добраны ещё 2 (после верификации, что на нём формально висят open):

- **SD-98** (паспорт акта + реестр актов) — закрыт. Дедуп документов качества (один документ
  на N позиций); реестр актов (управленческий) ≠ реестр-опись тома (листовой); выгрузка xlsx+pdf.
- **SD-105** (срок паспорта «истекает/истёк») — закрыт. Различать нужно, 4 состояния верны; но
  правило дат: срок релевантен ОТНОСИТЕЛЬНО ДАТЫ АКТА, не абсолютно; архитектуру сервер/фронт — Артёму.

По каждому — спавн `id-engineer` (sonnet) с чтением методички ИД, регламента и реальных томов;
черновик ответа базы; правки практика; оформление `[x]` + блок «### дата — <практик>» в
«Обсуждении» + резолюция (для закрытых).

## Продолжение 21–22.06 (follow-on бэкенда от Артёма/Кирилла)

После закрытия 5 базовых нот пришли уточняющие follow-on к реализации (механика: та же —
ответ базы из графа/памяти/предыдущих резолюций без нового субагента, практик подтверждает, запись):

- **SD-127 / SD-128 / SD-135** (создана 21.06) — закрыта. Развилки реализации после SD-105/102:
  - SD-127 (правило дат): «дата акта» для проверки документа = **дата окончания работ** (не подписания);
    режим **мягкий** (warning, не блок — том не тормозить из-за 1-2 паспортов); «истекает» = мягкое.
  - SD-128 (кросс-томный поиск): привязка документ→раздел РД уже задаётся при занесении в ПО (рассинхрона
    как в Молот/Треугольник не будет); кросс-томный — доп. поиск/фильтр по **материалу+поставщику**;
    группировка выше тома, термин нейтральный (блок/корпус/строение).
  - SD-135 (импорт спеки): **«было-стало» с подтверждением**; исчезнувшая позиция со связями →
    в **архив под контролем человека** (видимость + причина), не удаление.
- **SD-137** (создана 22.06, follow-on к SD-116 Q4) — закрыта. Процедура `reform` (перенумерация
  ПОДПИСАННЫХ актов): отдельный статус «требует переподписания»; старый скан → архив (доказательство
  приёмки, ADR-033); scope **узкий** (только перенумерация, разбивка акта — отдельно); обязательное
  текстовое **основание** → журнал/аудит; инициирует инженер ПТО (ролевой гейт follow-on);
  собранный том → пометка «требует пересборки» (пересборка вручную силами ПТО).

**Граф ИД запушен:** правки (мандаторность ВОР + правило дат) закоммичены и запушены в `origin/main`
claude-base (коммит `67bbf80`), `c48787a..67bbf80`. Уточнение SD-127 (дата = дата окончания работ)
добавлено в память `id_model_validated_corrections`, в граф пока НЕ вносилось.

**Музей — методичка-источник:** копии графа в Музее нет (`graphify/` пустая); первоисточник графа —
`…\Музей\Claude\Методология\Обучение составления ИД.md`. Правило дат в ней уже есть; мандаторность ВОР
явно не прописана. Предложено точечное примечание про ВОР — пользователь сказал **«стоп по графу»**,
методичку НЕ трогали (в backlog).

## Источники

- Граф ИД: `~/.claude/blocks/pto/skills/id-volume-graph/` (переехал в блок pto; память про
  путь `~/.claude/skills/...` устарела). Запуск query — только через PowerShell (UTF-8), не Bash.
- Методичка ИД + регламент «Порядок работы по тому ИД» + реальные тома в папке Claude объекта.
- Ноты vault в `60 - Текущая работа/Открытые вопросы/`.

## Где сломалось / грабли

- **Кириллица в путях через Bash:** `python <кирилл-путь>` даёт mojibake в argv (cwd-независимый
  скрипт графа не запустился). Решение — PowerShell с `chcp 65001` + `PYTHONUTF8=1` + call-оператор
  `&`. Read/Glob/Edit с кириллицей работают штатно (ломается именно argv для native exe в Bash).
- **SendMessage недоступен** в этой среде (нет в deferred tools) — продолжить того же субагента
  нельзя, каждый вопрос = свежий спавн id-engineer (перечитывает методичку).
- Граф методички (BFS) не содержит семантики перенумерации (это NOT IN DOCS) — выводили из
  принципов (каскад, сквозная нумерация, «факт первичен», ADR про скан-истину).

## Уроки (валидация базы — главный результат)

База как каркас рабочая, но customer zero выявил **2 содержательные неточности** + 1 пробел:
1. **ВОР сужена до АОСР** — на деле ВОР у КАЖДОГО акта, закрывающего работы (вкл. АОМР, рядом
   с ВСО). ВСО только у АОМР. АОУСИТО/испытания — без ВОР/ВСО. → правка графа ИД в backlog.
2. **«Поз.» РД** база видела как навигацию — на деле это движок «было-стало» РД↔ИД (diff
   позиций), которого в базе нет. Номера РД и ПВ не совпадают — норма.
3. Подписанный акт: база переоценила стоимость переподписания — при принятых работах
   переформирование ИД (разбить/добавить акт) даёт лёгкое переподписание.

Дополнительно (SD-98/SD-105) практик добавил то, чего в базе нет:
4. **Правило дат** — срок документа качества релевантен относительно ДАТЫ АКТА, не абсолютно
   (флаг «истёк» в хранилище справочен; валидация — при привязке паспорт↔акт).
5. **Привязка паспорт→позиция РД→ПВ→акт** — осознанная задача ПО, которой нет в ручной практике
   (там документ просто подшит за актом); не баг модели, а ценность ПО.

Вывод: доменный агент даёт качественный каркасный ответ из методички и реальных томов, но
финальная сверка с практиком обязательна — он ловит сужения модели и продуктовую специфику,
отсутствующую в графе. Уточнения сохранены в память; правку графа по мандаторности ВОР внести
при следующей актуализации.

## Остатки / backlog

- ✅ Правка графа ИД (`id_volume_extract.json`): мандаторность ВОР + правило дат — внесена, rebuild 46/93, auditor PASSED, **запушена** в origin/main (`67bbf80`).
- 🔸 Музей-методичка (`Методология/Обучение составления ИД.md`): синхронизировать мандаторность ВОР с графом — **отложено по решению пользователя** («стоп по графу»). Правило дат в методичке уже есть.
- 🔸 Граф ИД: уточнение SD-127 (дата проверки = дата окончания работ) пока только в памяти, в граф не вносилось — внести при следующей актуализации.
- Баг данных vault: `assignee` `daniil` vs `daniel` — унифицировать (пользователь сказал «не важно», низкий приоритет).
- ✅ Все вопросы зоны Даниила (`assignee: daniel`) закрыты: SD-116, SD-100, SD-102, SD-98, SD-105, **SD-127/128/135, SD-137**. Открытые ноты — только вне его зоны.
- ✅ Часы записаны: **W25 ~11 ч** (сб 20.06 валидация + вс 21.06 правка графа/SD-127-128-135), **W26 ~2 ч** (пн 22.06 SD-137).
