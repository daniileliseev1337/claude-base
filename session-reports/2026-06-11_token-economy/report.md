# 2026-06-11 — Токен-экономия: гейт сессий, матрица моделей, телеметрия

Вводная: `docs/2026-06-10-token-crisis-input.md` (инцидент 100M токенов/день на хабе).
Контекст: Fable 5 доступна до 22.06 — нужно использовать, не выбивая 5ч-лимиты.

## Что сделано

1. **CLAUDE.md, новая секция «Токен-дисциплина»** (в CORE):
   - сессионный гейт (видимая строка, как роутинг-гейт): смена темы / 30+ ходов /
     3+ субагентов / 5+ больших выводов → обязан предложить handoff;
   - матрица моделей субагентов: haiku (экстракция/fan-out, батчи ≤6) →
     sonnet (генераторы, форматные ревьюеры, norm-lookup) → модель сессии
     (только точечно: аудит больших ставок, координация, повторный фейл ревью);
   - fan-out 3+ на модели сессии запрещён без явного «да» (озвучить стоимость).

2. **Телеметрия восстановлена.** PostToolUse-хук был потерян (tool-usage.jsonl не
   писался с 10.06, хука не было ни в одном конфиге). Новый скрипт
   `scripts/tool-usage-track.ps1`: пишет jsonl + каждые 75 вызовов сессии подсказывает
   Claude предложить handoff (additionalContext). Зарегистрирован в **settings.local.json**
   (обкатка на хабе, в shared не уезжает). Ротация лога на 5 МБ.

3. **Мониторинг: ccusage** (npx ccusage daily / blocks). Разбирает локальные
   транскрипты, показывает расход по дням/моделям/5ч-окнам, cache отдельно,
   у активного окна — прогноз выгорания. `ccusage blocks --live` — живой монитор окна.
   Замер подтвердил диагноз: 08-10.06 — по 100-140M в день, **cache reads = 91%**
   (315M из 345M за 4 дня); лимит 5ч-окна ≈ 69M токенов (окно 09.06 06:00 = 100%).

4. **Ревизия плагинов/MCP.** CLI: из 11 установленных плагинов включены только 2
   (superpowers, claude-md-management) — выключенные веса не дают, удалять не стал
   (Karpathy #3). Глобальные mcpServers = эталон манифеста (11 core + autocad +
   firecrawl) — не трогал. Тяжесть desktop-сессий — от коннекторов приложения:
   дубль Desktop Commander (MCP + плагин), Windows-MCP при наличии встроенного
   computer-use, Autodesk Knowledge MCP. Файлово не правится — клик-лист пользователю
   (Settings → Extensions/Connectors в desktop-приложении).

## Замеры префикса (на каждый запрос каждой сессии)

- description 18 агентов: ~50.5k символов (~20-25k токенов);
- 19 SKILL.md frontmatter: ~24k символов (~10k токенов);
- CLAUDE.md: ~21.6k символов (~9k токенов). Итого с системным промптом ~60-80k токенов.

## Уроки

- Хуки могут теряться при мержах конфигов — телеметрию проверять фактом записи, не верой.
- weekly-лимит 39% при двух выбитых 5ч-окнах = проблема пиковой интенсивности:
  дробление по сессиям решает большую часть без инженерии.
- Каждая строка CLAUDE.md/description оплачивается в каждом запросе — правки в CORE
  держать компактными; диета description — только по телеметрии молчащих агентов
  (роутинг живёт на этих фразах, резать вслепую нельзя).

## Хвосты

- Диета description агентов + ревизия скиллов — после 22.06, по данным tool-usage.jsonl.
- context-ledger — изучить отдельно, без спешки.
- Обкатанный хук-счётчик → кандидат в shared settings.json через /sync-base.
- pull-feedback прогнан: 8 веток в feedback-inbox/all/ — ревью отдельной сессией.
