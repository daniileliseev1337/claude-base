# Сессия 2026-07-02 — Блок 3 реворка: унификация PDF-слоя

## Что сделал
Исполнен Блок 3 плана фиксов после ревизии Fable 5 (`ПЛАН_фиксы-после-ревизии_2026-07-02.md`
в папке программы реворка). Развилка владельца решена первым шагом: **узкий pdf-edit**, не ретайр.

1. **pdf-helper → pdf-edit** (`git mv`, история сохранена). SKILL.md переписан:
   - вырезано извлечение (скан-проверка, OCR-секция, «Извлечение таблицы», ловушки
     «сдвиг колонок таблиц» и «CID/PScript-Distiller», extraction-триггеры description);
   - editing сохранён дословно (сверено git-diff со старым файлом): Inkscape-правка
     вектор-PDF, перерисовка нанесённой разметки, merge/split, страницы, AcroForm,
     перекраска облачков `/AP`, watermark, визуальный diff, вызов pdf-reviewer;
   - добавлена секция «История» (бывший pdf-helper, что и куда вырезано).
2. **Extraction-артефакты в doc-extract**: `tools/extract_drawing_meta.py` + тест
   (2/2 passed после переноса), `references/drawings.md` (импорт-путь починен).
   Попутно закрыт пункт pdf-helper в `memory/backlog_tools_layer_migration.md`
   (scripts/ → tools/).
3. **doc-extract = единственный вход извлечения**: description усилен («ЕДИНСТВЕННЫЙ вход»,
   впитал триггеры «извлечь из pdf», «разбери pdf», «штамп чертежа», «слои pdf»,
   «прочитай скан-PDF» + негативное разграничение), в теле добавлены пункт дерева
   «мета чертежа-PDF», секции «Ловушки извлечения» и «Соседи (разграничение)»;
   битая ссылка на 0_СТАТУС_программы.md убрана.
4. **image-text-replace**: «после pdf-helper» → «после doc-extract» (description),
   pdf-edit в связках тела и README.
5. **co-verify**: разграничение с doc-extract (координатная сверка/diff СО ≠ общее
   извлечение) в description + «Чего НЕ делает».
6. **Живые ссылки по базе** (11 файлов): agents pdf-reviewer (правка → pdf-edit),
   снабженец и expertiza-responder (парсинг PDF → doc-extract), chains
   pdf-scan-extract / design-stamp-corrections, upd-parser, pd-tep-extractor
   (SKILL + комментарий tep_validate.py), skills.md, docs/methodichka.md, README.md,
   «Карта vault.md». История (CHANGELOG, session-reports, harvested) и
   авто-генерённый graphify-out — не трогались намеренно.

## Ревью
`auditor` (sonnet), чек-лист A–G: **PASSED**. Проверено: extraction-триггеры только
у doc-extract (у соседей — допустимые доменные/негативные); кросс-ссылки взаимны у всех
4 скиллов; 0 живых ссылок на pdf-helper вне намеренной истории; editing-рецепты не
потеряны (построчный diff с `git show` старого файла); пути существуют, тесты проходят;
CLAUDE.md не изменён (git status/diff/log). 2 MINOR: `__pycache__` в tools/ (удалён,
gitignore покрывает), расхождение в подсчёте замен (4 vs 5 — только счёт, не содержание).

## Где сломался / трение → разобрано и починено в той же сессии
- **Auto-sync коммитил посреди задачи**: правки уехали в base четырьмя фоновыми
  auto-sync коммитами ещё ДО вердикта ревьюера. Разбор фактами (зонд stdin хуков +
  живые тесты): виновник — НЕ субагенты (они SessionStart/End не стреляют вовсе;
  прежнее INFO п.1.2 опровергнуто), а короткоживущие headless-инвокации
  (one-shot `claude -p`/хелперы): полный SessionStart→SessionEnd за 2–8 с, чей
  SessionEnd свипал весь dirty whitelist. ФИКС: гейт эфемерных сессий в
  auto-push.ps1. Аудит v1 NOT PASSED (однофакторный порог «<10 строк» ловил
  9-строчный транскрипт; гейт глушил consumer-ветку) → v2: ДВУХФАКТОРНЫЙ
  критерий (строк <10 И длительность <60 с по timestamp'ам транскрипта;
  «контрпример» оказался стабом 0.118 с), гейт после consumer-ветки,
  телеметрия real SessionEnd, fail-open по всем веткам. Тесты 5/5, живой
  one-shot ловится, ре-аудит PASSED (0.118 с пересчитан аудитором независимо),
  MINOR (двойной Pop-Location, предсуществующий) закрыт. Остаточные риски
  задокументированы в ПЛАН-файле программы: свип настоящей параллельной
  сессией (лечение архитектурное — push из ритуала отчёта, решение владельца)
  и теоретическая быстрая сессия с правками (отложенный push, без потери).

## Уроки (дополнение по гейту)
- Гипотеза «SessionEnd стреляет на субагентах» держалась две записи в статусах —
  опроверг только живой зонд. Payload хука надо было поймать фактом с самого
  начала (зонд stdin = 15 строк, окупился мгновенно).
- Однофакторные эвристики («строк < N») бьются о реальные данные — аудитор
  нашёл контрпример поиском по машине; двухфакторность (объём И время)
  устойчивее. Сам контрпример при измерении оказался стабом — мерить, а не
  спорить.
- Телеметрия-гейт «75 вызовов» сработал посреди сессии при нормальном контексте —
  живое подтверждение диагноза Блока 2 (счётчик вместо веса).

## Уроки
- Резка скилла «извлечение/редактирование» держится на одном вопросе к каждой секции:
  «этот рецепт ЧИТАЕТ или МЕНЯЕТ файл?» — граница проводится без остатка.
- Проверку «рецепты не потеряны» правильно делать git-diff'ом со старым файлом
  (`git show <commit>:<old-path>`), а не памятью — auditor так и поймал, что всё цело.

## Осталось
- Блоки 2 и 4 плана — отдельными сессиями.
- graphify-скелет пересоберётся staleness-хуком сам (пути skills/pdf-helper/* в графе
  устареют до пересборки — некритично).
