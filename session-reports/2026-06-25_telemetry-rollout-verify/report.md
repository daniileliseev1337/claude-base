# Session report — 2026-06-25 — раскатка/верификация телеметрии + оценка spec-pilot

Контекст: программа реворка базы («Заведомо проигранный бой»). Запрос владельца — «раскатываем» (телеметрию инструментов).

## Что сделано

1. **Телеметрия (PostToolUse `log-tool-usage.ps1`) — оказалась УЖЕ раскатанной и рабочей.**
   - Память программы (диагноз 2026-06-25 день) утверждала: «PostToolUse-хук не зарегистрирован нигде, jsonl отсутствует». **Проверка фактом опровергла:**
     - блок есть и в `settings.shared.json`, и в активном `settings.json`;
     - `~/.claude/.local-state/tool-usage.jsonl` пишется в реальном времени (записи текущей сессии).
   - Причина расхождения: блок добавила предыдущая сессия (коммит `01da531`, 20:40) ПОСЛЕ записи диагноза → диагноз устарел за тот же вечер.
   - Обновил память программы (`0_СТАТУС_программы.md`, `_ПАМЯТЬ_программа-реворка.md`) — заменил устаревший диагноз фактическим статусом с пометкой опровержения.

2. **Push на удалёнку (раскатка на ноут).**
   - `01da531` висел unpushed. Push отклонён как non-fast-forward: локальный `main` отстал от `origin/main` на 4 коммита с DANIIL-LAPTOP (параллельная работа вечером).
   - Расхождение чистое (`settings.shared.json` ноут не трогал) → `pull --rebase` (1 коммит поверх 4, без конфликтов) → push `ec59465`. Блок уедет на ноут следующим auto-pull.

3. **Оценка кандидата `spec-pilot.zip`** (лид-магнит к ролику «6 фраз для Claude Code»).
   - Вердикт: **отклонить как установку.** Описывает порядок интервью→спека→параллель→проверка, который у нас уже реализован и жёстче (brainstorming/domain-grilling, роутинг-гейт, Токен-дисциплина с гейтом на субагентов, verification + обязательный ревьюер). Установка = новая ось дубля против принципа унификации. Уровень потребительский, без Tools-слоя.
   - Ценность ограниченная: педагогика («6 фраз», CHEATSHEET) для онбординга + третья независимая валидация нашего порядка. По решению владельца — не заносить в трекер.

## Где сломался / грабли

- **MAX_PATH при распаковке zip в scratchpad.** Полный путь scratchpad (кириллица в home «Даниил ПК» + вложенность архива) превышал лимит → Expand-Archive молча не создавал глубокие папки; short-name `88F3~1` нестабилен между PowerShell-вызовами и не резолвится для Read tool. Обход: распаковка в короткий `C:\Temp\sp`.

## Уроки

- **Статус инструмента судить ЗАПУСКОМ/проверкой файла, а не записью в памяти** — диагноз устаревает за часы (повтор урока про graphify). Память программы догоняет реальность с лагом.
- **`/clear` не триггерит SessionEnd → ни auto-push, ни session-report.** Коммит сессии остаётся локальным и unpushed (ровно это и расхлёбывали с `01da531`). Закрывать сессию нормальным выходом, не `/clear`.
- При длинном кириллическом home распаковку/временные файлы вести в коротком ASCII-пути.

## Артефакты

- Память программы обновлена: `Desktop\Claude\Заведомо проигранный бой (или нет)\{0_СТАТУС_программы.md, _ПАМЯТЬ_программа-реворка.md}`.
- Git claude-base: `ec59465` (push выполнен).
