# Session report (доп.) — <объект-Б> СС блок1 эт.6: финальная расстановка v3 (СВЕРЕНО С PDF)

Продолжение `report.md`. Полный тех.контекст — память `project_blsh_ss_model_study`.

## TL;DR
- Перепривязал СПС блок1 эт.6: **114 приборов, ID 811146-811261, откат `placed_v3.txt`**.
  Состав RCU31/LUM28/LUS4/BCU24/PCU19/WCCU4/NCP4. Факт=расчёт (0 дрейфа/поворота), Z верны, ws513.
- Это ВТОРАЯ переделка за сессию. Первая (v2, 115) залита по эвристике → пользователь поймал,
  что я НЕ смотрел реальный PDF. Откатил, переделал правильно.

## Что пошло не так в v2 (урок)
- v2-состав брал «ближайшей меткой нужного класса» из plan16-экстракта + сам себя проверял
  (placements↔placed = тавтология), реальный PDF ГЛАЗАМИ не открывал. Лампы поставил у двери
  палаты (надо — в коридоре), клизменные недоукомплектовал, 2 палаты без ламп пропустил.
- Пользователь (справедливо) развернул: «расставляешь по правилам → сверяешься с PDF →
  убеждаешься сам → просишь утвердить».

## Правильный метод (v3), сработал
1. **Индекс-группировка кодов** `PT/LM/SVU/MV/TM 6.1.N` (`index_group.py`) — детерминированный
   состав без догадок: 31 RCU, 32 лампы (28 палат + 4 спец), 44 SVU, 4 WCCU, 4 NCP.
2. **Визуальное чтение реального PDF** (лист 34 <шифр-чертежа> Изм.8): Workflow по 15 регионам с
   независимой перечиткой (31 агент, ~58 мин, 2M токенов) → device→комната+позиция+H.
   ⚠ Workflow ошибается на стыке секций/в админ-зоне (свалил sec08-приборы в 6.09.010,
   PT6.1.11 не туда) + недочитал правую часть 6.08 (я задал регионы уже листа). Грабли:
   лист 34 = ВЕСЬ блок1 эт.6 (план шире, x до ~0.7).
3. **Реконсил** (`reconcile.py`/`extract_wf.py`): 91 надёжная привязка + ручное дочтение
   пробелов (юниты 16-20/31/34, секция 6.09) поверх ошибок Workflow → `final_devices.json`.
4. **place_v3.py**: позиции по геометрии Revit. Главная правка — **ЛАМПЫ В КОРИДОР**
   (марш из двери наружу до corr-комнаты). RCU у двери, BCU/PCU у сантехники по H, WCCU низко,
   NCP у поста (анкер по подсобному — посты в Revit-AR нумерованы иначе).
5. Залив батчами ≤38 (FailuresPreprocessor, Rotate, Z=offset), факт-сверка из Revit.

## Состояние
- Модель `027_МЕД-БЛШ_СС2` — 114 приборов в наборе СС2_СПС, НЕ сохранена (пользователь смотрит/сохраняет).
- Скрипты в `C:\Temp\blsh_sps\`: index_group, extract_wf, reconcile, place_v3, plot_v3, cmp_v3,
  final_devices.json, placements_v3.json, verified_composition.md, index_findings.md.
- Планы: `v3_6_07/08/09.png`.

## Уроки (процессные)
1. **СВЕРКА = смотреть РЕАЛЬНЫЙ источник глазами, а не свой экстракт.** Проверка расчёта самим
   собой = тавтология, не верификация. (Karpathy #4 + #5: не подхалим — пользователь был прав.)
2. **Workflow визуального чтения — мощно, но проверять на стыках/в нестандартных зонах** и
   точно задавать регионы (промерить реальные границы листа ДО запуска).
3. **Детерминированная структура (коды) + визуал = надёжнее любой одной эвристики.**

## Осталось
- Этажи 1-5,7-9 блока1 (метод и скрипты переиспользуемы: входные JSON геометрии этажа +
  визуальное чтение листа этажа), затем блоки 2,4 (блок 3 — эталон).
