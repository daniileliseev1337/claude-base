# Session report — 2026-07-04

**Темы:** (1) R&D «этаж-2 — Claude-управляющий сессиями» (папка программы, НЕ база);
(2) правка базы — поведенческий hook про чтение мелкого с изображений/экрана.

---

## Часть 1 — этаж-2 (папка программы)

### Сделано
- **Задача 1 (reconcile).** `checkers.reconcile_with_verdict1` перестал клеймить
  эскалацию-ИНФОРМИРОВАНИЕ (судья-1 законно эскалирует факт prompt-injection в артефакте
  при чистом floor) как «судья-1 ПЕРЕУСЕРДСТВОВАЛ». Ярлык остаётся только при реальном
  обвинении воркера (truthful=false / task=no / score≤5); вызов судьи-2 при эскалации
  сохранён отдельным триггером (`should_call_judge2`). Свидетельство — прогон
  `runs/20260704-001134` (совет 3/3 подтвердил обоснованность эскалации на кейсе инъекции).
- **Приёмка:** новый переносимый `test_reconcile.py` (floor задаётся dict, транскрипты не
  нужны → идёт на любой машине). **17/17**.
- **Задача 2 (параллельные воркеры) — draft.** `manager_parallel.py`: N воркеров разом,
  каждый в своей `runs/batch-<метка>/w<i>/sandbox` (изоляция → честная атрибуция; общая
  песочница = гонки за report.md = баг кейса №4). `manager_poc` параметризован под
  per-worker sandbox (обратно совместимо). `--dry-run` самодостаточен (стаб без SDK/цербера)
  — доказал изоляцию за 0 токенов. Батч-процедура вписана в ИНСТРУКЦИЯ_УПРАВЛЯЮЩЕГО.
- **Переносимость:** хардкоды `C:\Users\<user>\...` → `Path.home()` в 4 файлах
  (manager_poc TOOLS/PROJECTS, test_checkers PROJECTS, adjudicate, regen_live_facts).
  7/7 компиляция, 0 остаточных хардкодов.

### Не сделано (бэклог этаж-2)
- Живой батч воркеров — требует approve (деньги + 5ч-лимит) и исходной станции (цербер там).
  draft-then-approve.
- Приёмка checkers/judge2/adjudicate/council/test_reconcile в общий `supervisor/tools`
  (обезличить перед base).

### Уроки
- **Промпт сессии опережал диск:** рапортовал часть 3 «полностью закрытой» + прогон 07-04,
  которого на диске СПЕРВА не было; досинхронизировалось по ходу (Я.Диск). Verifiable-first
  (Test-Path/Grep вместо доверия промпту) вскрыл реальное состояние — не строить на рапорте.
- **Поиск в нативной кодировке:** стартовое «на диске ничего нет» — ошибка: искал латиницей
  `etazh2`, а папка кириллицей `этаж2`; пути промпта были огрублены. Пути в промпте — ориентир,
  не буквальность; имена искать в родной кодировке.
- **Я.Диск перезаписывал файлы mid-session** (`test_checkers` «modified since read» дважды) —
  перечитывать перед каждой правкой.

---

## Часть 2 — правка базы: hook A11.1 (чтение мелкого с изображений)

### Задача (владелец)
При ЛЮБОЙ попытке скрина поднимать разрешение У ИСТОЧНИКА (нативный зум в ПО / текст-слой PDF
через pdf-mcp / OCR), а не всматриваться в низкоразрешённый растр и не делать цифровой зум
готового кадра (пиксельная каша → ошибки). Живой пример — `pass_p4.png`: разворот PDF-паспорта
оборудования ужат в мелкий PNG, текст = сплошные пиксели.

### Сделано (запушено в claude-base: 708f431, 306865f)
- **hook** `scripts/screenshot-source-zoom.ps1` — PreToolUse, matcher `screenshot|zoom`,
  инжектит additionalContext-напоминание. Не блокирует (обзорный скрин как обычно).
- **Место — источник, а не следствие:** регистрация в `settings.shared.json` (strictly-shared,
  вливается `merge-shared-settings.ps1` в локальный settings.json на каждой машине). Прямая
  правка `settings.json` эфемерна (см. урок).
- **Правило A11.1** в `anti-patterns.md` (новая Категория 11).

### Уроки
- **`settings.json` gitignored** (Phase 1 sync-redesign, 2026-05-21): личный, UI-driven,
  регенерится; hooks — strictly-shared → любая ручная правка settings.json откатывается merge.
  Правки хуков/shared-значений идут в `settings.shared.json`. Улику дала ручная
  `git add settings.json` → «paths are ignored by .gitignore».
- Новый event-ключ hooks активируется в текущей сессии только после `/hooks` или нового чата
  (settings-watcher).
- matcher `screenshot|zoom` не ловит Chrome-скрин через `mcp__Claude_in_Chrome__computer`
  (скрин внутри мультиэкшена) — бэклог.

---

## Harvest — готовые инструменты чтения мелкого
Разведка (sonnet-субагент, метрики верифицированы через api.github.com):
- **region-screenshot HD:** готового MCP с приличными метриками нет (Windows-MCP 6.3k★ — только
  full-screen + scale, региона нет; MCPControl 326★ — region есть, но ~7 мес без коммитов).
  → свой скрипт на `mss` (захват региона в физпикселях, ~10 строк) — бэклог.
- **image-tiling для VLM:** `keiver/image-tiler-mcp-server` (1★) есть, но перекрывается 5
  строками Pillow (crop-плитки). Готовый MCP оправдан только ради пресетов+entropy-фильтра.
- **PDF/OCR:** закрыто уже стоящим (pdf-mcp текст-слой / mineru / image-text-replace).

Вывод: суть закрыта hook'ом (поведение) + встроенными MCP; единственный пробел (region-capture
HD для computer-use в AutoCAD/GUI) — проще свой мини-скрипт, чем внешний инструмент.

---

## Бэклог (следующие сессии)
1. **этаж-2:** живой батч воркеров (approve, деньги+лимит, исходная станция); приёмка инструментов в базу.
2. **hook:** `computer` в matcher (Chrome-скрины) — если понадобится.
3. **tools:** `region_screenshot.py` (mss) + Pillow-тайлер — если региональный HD-capture понадобится (`pip install mss`).

## Промпт для нового чата
> Продолжение. Сделано 2026-07-04: этаж-2 — reconcile-фикс + `test_reconcile` 17/17 + draft
> `manager_parallel` (dry-run, изоляция песочниц) + переносимость путей (Path.home()); правка
> базы — hook A11.1 (PreToolUse `screenshot|zoom` → зум у источника, в `settings.shared.json`,
> запушено). Дальше по бэклогу (выбрать): живой батч воркеров этаж-2 (approve — деньги+лимит,
> на исходной станции) · приёмка этаж-2 в общий supervisor/tools · `tools/region_screenshot.py`
> на `mss`. Перед стартом — STOP-процедура CLAUDE.md полностью.
