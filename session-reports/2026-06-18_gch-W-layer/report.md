# Сессия 2026-06-18 — Блок ГЧ, W-слой (генератор xlsx → ACAD_TABLE)

Продолжение блока ГЧ (третий блок семьи pto). Предыдущая сессия: 2026-06-18_gch-block-anatomy
(этап 0 «анатомия ИС» + прототип R-слоя). Эта сессия: **W-слой — первый бой пройден.**

## Задача
Инструмент «xlsx → ACAD_TABLE» через autocad-mcp, заменяющий OLE-таблицы ВОР на нативные
ACAD_TABLE. Жёсткое требование: таблица вмещается в лист (адаптивно под разные листы/схемы).
Первый бой: ВОР монтажа оборудования, лист «Об 1» тома Р-4-ВиВ (<объект>).

## Результат — PASSED
Таблица «Об 1» (15 позиций) построена как нативная ACAD_TABLE на штатном месте удалённого OLE,
DWG сохранён (бэкап рядом). Верификация количеств: все 15 значений колонки «Всего» совпали с xlsx.
- кириллица end-to-end, дюймы `1/2"` (файловый ввод данных — кавычки не ломают LISP),
- автоперенос длинных наименований + адаптивная высота строк,
- merged-секции «Пом. …» на всю ширину, выравнивание (№/Ед/Всего — центр, Наименование — влево),
- ГОСТ-шрифт (isocpeui) чертёжного вида,
- **adaptive auto-fit**: бинпоиск максимального textheight под высоту зоны (H=93.4 при target 95).

## Инструмент (в `ГЧ_прототип/`, кандидат в `skills/id-scheme-toolkit/tools/`)
Двухслойная архитектура (Karpathy «код в tools/»):
1. **`xlsx2acadtable.py`** — режет лист по строкам «Ведомость объёмов работ» на таблицы,
   классифицирует строки (title/header/section/data), опц. нормализует нумерацию (№ + 1..N),
   пишет `C:/temp/table_data.txt` (cp1251, TAB-разделители). Параметры argv: layout, ins x/y,
   cols csv, style, thmax, margin, target_h, renumber.
2. **`gen_table.lsp`** — статичный ASCII-ридер: парсит data-файл, строит ACAD_TABLE
   (vla-AddTable в paperspace листа), SetText/MergeCells/SetCellTextStyle/SetCellTextHeight/
   SetRowHeight/SetCellAlignment, **бинпоиск textheight под target_h** (c7:build).
   Кириллица приходит из cp1251-файла, не из кода → кавычки/спецсимволы безопасны.

## Анатомия целевого xlsx (лист «М об»)
Содержит ДВЕ таблицы ВОР монтажа оборудования: r1–r96 (этаж −1, пом. −1-1-XX, ~88 поз → лист
«Об -1») и r97–r114 (этаж 0, пом. 0-1-XX, 15 поз → лист «Об 1»). Колонки: A №(узк), B
Наименование(шир), C Ед.изм, D Всего. Секции-подзаголовки = merged A:D. Аномалия источника:
в шапке колонка № = «1», нумерация позиций сдвинута (с 2) — по решению пользователя нормализуем.

## Анатомия целевого DWG (см. также ГЧ_прототип/dwg_scan_W.md)
20 layouts. Листы оборудования: «Об -1» (А1), «Об 1» (А3), «Об акс». Table style — только
Standard (штамп Треугольника НЕ ACAD_TABLE, в отличие от Молота). «Об -1»: 5 OLE = одна большая
таблица, разложенная вручную в 4 колонки (зачем нужен авто-перенос колонок). «Об 1»: 1 OLE 90×102.

## Грабли / уроки (КЛЮЧЕВЫЕ — в память)
1. **`vla-get-PaperSpace` ненадёжен** для AddTable/перебора → Automation error. Брать paperspace
   через перебор `(vlax-for lo (vla-get-Layouts doc))` → `(vla-get-Block lo)`. (То же с vla-Item.)
2. **`vl-catch-all-apply` требует ВТОРОЙ аргумент-список**: `(vl-catch-all-apply 'fn (list a b))`,
   НЕ `(vl-catch-all-apply '(lambda()...))`. Иначе ошибка эскалирует мимо catch.
3. **Высота строки ACAD_TABLE не сжимается под мелкий текст сама** — начальная rowHeight из
   AddTable фиксируется. Явно `vla-SetRowHeight` (минимум = th*1.4+2*margin); перенос растит выше.
4. **Adaptive auto-fit = бинпоиск** th, не линейная коррекция (высота нелинейна из-за переносов
   → линейная проскакивает вниз, недоиспользует зону).
5. **Кавычки/дюймы в данных** ломают LISP-литералы → данные через cp1251-ФАЙЛ, ридер на ASCII.
6. **`(open file)` в LISP при падении виснет дескриптором** → следующий open того же файла = nil
   → "bad list". Собирать результат в payload, не писать файл LISP'ом в цикле отладки.
7. **`vla-HandleToObject` на невалидный handle = Automation error ВНЕ catch** (если в списке
   аргументов другого catch). Оборачивать сам HandleToObject (см. c7:safe-del).
8. **Копировать .lsp в ASCII-temp ПОСЛЕ КАЖДОЙ правки** — load кэша нет, но забытая копия =
   отладка старой версии (потерял ~3 итерации на "numberp nil"/"no function").
9. execute_lisp контекст (defun/setq) сохраняется между вызовами в рамках сессии file_ipc.
10. PNGOUT всего листа → мелкий текст рендерится чёрными блоками; для оценки текста — ZOOM Window
    на bbox таблицы, для оценки вмещения — ZOOM Extents.

## Состояние артефактов
- `ГЧ_прототип/gen_table.lsp`, `xlsx2acadtable.py` — рабочий генератор (НЕ обезличены, привязаны к файлу).
- `ГЧ_прототип/dwg_scan_W.md`, `pv4_*.md` (в C:/temp) — разведка.
- Боевой DWG сохранён; бэкап `..._BAK-W-20260618.dwg` рядом.
- В claude-base пока ничего (блок ГЧ ещё не оформлен).

## Второй бой — «Об -1» (multi-column) — PASSED
Та же сессия. Доработан multi-режим: `c7:build-multi` + `c7:build-list` (рефактор build-once на
произвольное подмножество строк). Алгоритм: измерить высоты строк пробной таблицей →
`c7:layout-cols` (жадная раскладка по колонкам target=высота зоны, повтор title+header,
перенос висячей секции) → бинпоиск шрифта чтобы колонок ≤ MAXCOLS → построить K таблиц рядом.
Результат: 88 позиций → **3 колонки, шрифт 2.47 мм** (зона 517×315 на А1, maxcols=4 — запас).
Верификация: все **88/88** количеств совпали с xlsx (`C:/temp/verify_obm1.py`). 5 OLE заменены,
DWG сохранён. build-multi отработал быстро (таймаута НЕ было, опасения не подтвердились).
Косметика: подколонку «Всего» расширить (9→12), иначе «Всег/о» переносится в шапке.

## Правка после ревью пользователя (Об -1) — КЛЮЧЕВОЙ урок размещения
Пользователь: «вставил верно, но перекрыл весь чертёж, нечитабельно». Причина: (1) ACAD_TABLE
ПРОЗРАЧНА (OLE были непрозрачными картинками) → план/выноски просвечивают + текст накладывается;
(2) auto-fit максимизировал шрифт (2.47) → таблица раздулась вниз на выноски/план.
Исправление по указанию пользователя («перенести в правый верхний свободный угол, не влезает —
уменьшать»): компактный режим thmax≤1.5, прижать колонки ВЛЕВО от правого края
(x0=xright−K·colw−(K−1)·gap), верх опустить под угловой штамп «Приложение к АОСР» (y≈499.6 →
ytop=490). + балансировка колонок (target_bal=total/K: было 79/9 → стало 45/43). Итог: 2 равные
колонки x[−259..−3] y[337..490], план/выноски/приложение свободны. DWG пересохранён.
Грабли: координаты прокси-MText роняют LISP "bad list" через vla (bbox/InsertionPoint/entget-via-vla)
→ брать `ssget "_X"` + DXF assoc.

## Автопоиск свободной зоны (готов, подтверждён на Об -1)
`find_free_zone.py` — РАСТРОВЫЙ детектор (надёжнее векторного). LISP: zoom extents, getvar
VIEWCTR/VIEWSIZE, PNGOUT чистого листа. Python: бинаризация → downsample K-сетка (+clearance) →
max-empty-rectangle (гистограммный) → пикс→коорды (mmpp=VIEWSIZE/px_h, x_min=ctrx−model_w/2).
На Об -1 вернул x[−426..−4] y[262..488] maxcols=3 — совпало с ручным правым верхом, обошёл
приложение/выноски/план. Маппинг точен (model_w=854≈A1+поля). Подтверждено рендером (зазор до
приложения к АОСР есть). Координаты прокси-MText — через ssget+DXF (vla роняет «bad list»).

## Оформление блока (done, хаб)
- `blocks/pto/skills/id-scheme-toolkit/` — SKILL.md + tools/ (xlsx2acadtable.py с импортируемым
  parse_table, gen_table.lsp с c7:build/c7:build-multi/c7:verify-dump, find_free_zone.py,
  verify_table.py) + .gitignore. Все py компилируются, smoke verify 88/88 PASSED из блока.
- `blocks/pto/chains/id-scheme-cascade.md` — каскад исп. схем (W-слой), QA-шаг = ревьюер графики
  (verify значений + рендер-сверка вмещения/перекрытий).
- `BLOCK.md` — состав + разграничение триггеров обновлены.
- НЕ закоммичено (хаб; коммит/пуш по запросу пользователя).

## block pto (feedback-абзац)
Каскад: id-scheme-cascade (W-слой), первое применение. Сработало: пайплайн автозона→данные→
build-multi→verify полностью прошёл на Об 1 (single) и Об -1 (multi, 88 поз). Ручное вмешательство
потребовалось: 1 цикл правки раскладки (прозрачность ACAD_TABLE + перекрытие приложения к АОСР —
пользователь поймал на рендере) → добавлены компактный режим, прижим к правому краю, балансировка
колонок, обход штампов. Урок в toolkit и SKILL.md. Автопоиск зоны убрал ручное задание угла.

## TODO (следующие сессии)
- **[п.3] Тест на остальных листах/томах** (М тр, М тп, Гильзы, Канализация) — те же tools,
  меняются layout/зона/table_index. Прогнать пайплайн целиком, отловить новые форматы ВОР.
- R-слой блока ГЧ (полнота схем: длины/диаметры/арматура/разрывы) — отдельно.
- Граф ГЧ: ось анатомии ИС в id-volume-graph (лист 1:1 с актом, VP/слои, OLE статичны,
  правило «количество не из выносок»).
- Коммит блока pto в claude-base (по решению пользователя).
- Оформить блок: `blocks/pto/chains/id-scheme-cascade.md`, `skills/id-scheme-toolkit/tools/`
  (LISP-экстрактор + анализатор R + генератор таблиц W), ревьюер графики (PNGOUT + экстракция +
  инвариант), smoke-тесты, feedback-абзац `## блок pto`.
- Граф ГЧ: ось анатомии ИС в id-volume-graph.
