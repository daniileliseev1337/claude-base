# Session Report: klimat-pro-AI — дизайн-фаза → репланинг (2026-06-25, host DANIILPC)

## TL;DR
- Вели дизайн-полировку дашборда через Claude Design; по ходу — много трения вокруг
  инструментов и **устаревшей инфы** (исправлено: см. ниже).
- Закрыли: A-4 (зелёный профиль → золото, **в проде и в git**), полная **актуализация
  аудита 11.06** против живого кода (recheck-карта), 7 дизайн-вариантов в Claude Design.
- **Реприоритизация владельцем:** дизайн паркуем (лимит времени/недельный лимит). Новые
  приоритеты — запуск сайта в работу, ИИ-модуль+дизайн для компании, MCP (личный+компания).
- **Задача новой сессии:** составить ПОЛНЫЙ инвентарь «что осталось сделать ВЕЗДЕ», затем
  приоритет/порядок владелец разложит вместе с ней.

## Состояние артефактов
- **Репо:** `F:\Сайт\redesign-v2-fresh` (имя репо `klimat-pro-AI`), рабочее дерево чистое.
- **origin/main = `f6a745d`** (запушено): A-4 + audit recheck + баннер. Прод и git сошлись.
- **Прод:** nginx WSL `127.0.0.1:8080` → публично `https://193-124-130-236.sslip.io`.
  Деплой: `npm run build` → `wsl bash -c "bash /mnt/f/*/redesign-v2-fresh/deploy/nextcloud/deploy-web.sh"`.
- **Claude Design** проект «Design System» `a56ef708-b532-4add-b9d6-654bc5db011d` (читает GitHub):
  - группа `Modals` — 4 варианта панели (calm-frame / hairline / full-premium / frosted-liquid)
  - группа `Header & Tabs` — 3 варианта (transparent-hairline / tinted-hairline / unified-glass)
  - источники локально (temp, могут очиститься — в Claude Design копии persist):
    `…\scratchpad\design-system\components\{modal-shell,header-tabs}\*\preview.html`
  - рендеры для просмотра: `C:\Users\Даниил ПК\{modals.png,headertabs.png}`.

## Что сделано (детали)
1. **A-4** — профиль-кнопка в шапке `App.jsx:8773-8801`: зелёный `#6ee7a8`/`rgba(110,231,168)`
   → золото `#e8c860`/`rgba(212,175,55)`. Функциональный зелёный (success/оплаты/задачи) НЕ тронут.
   Собрано, задеплоено (nginx отдаёт новый бандл `index-B163Aapd.js`, HTTP 200), закоммичено `f6a745d`.
2. **Актуализация аудита 11.06** → `docs/superpowers/audit/2026-06-25-audit-recheck.md` + баннер
   «устарело» в старом `2026-06-11-fable-site-audit.md`. Вердикт: **почти весь CRITICAL/MAJOR закрыт**
   редизайнами 19-23.06 (C1-C5, M1-M3, m1, m8 — проверены в живом коде). Реально **открыто только
   минорное**: m3 (бейдж на reconnect), m4 (`SET search_path` в `can_access_task`), m6 (`apartment-tracker/`
   уборка), m7 (`telegram-notify` мёртвый код), m5 (чистка данных БД), + спот m2 (`App.jsx:8417`).
3. **frontend-design** плагин включён durable: добавлен в `settings.shared.json`→`enabledPlugins`
   (merge-скрипт затирал личный settings.json — корень был там). Активен.
4. **Метод дизайна зафиксирован (гибрид):** системное (модалки/вкладки/акценты) — варианты на наших
   токенах делаю я → push в Claude Design → владелец правит живьём → перенос в код; свободная эстетика
   (логотип/liquid glass) — генерит Claude Design. Всегда: владелец рулит в Claude Design, я переношу+деплою.

## Уроки сессии (важно для новой)
- **Живой код/конфиг — единственный источник правды.** Датированные артефакты (аудит/отчёт/память/
  слова прошлого чата) = зацепка, НЕ факт. Сверять с кодом ПЕРЕД заявлением. (В этой сессии правило
  недоприменялось → стыд с «устаревшей инфой».)
- **Claude Design — основной инструмент дизайна, не принижать.** Round-trip: владелец правит пиксели сам.
  File-sync через тул `DesignSync` работает; просмотр синк-карточек в вебе, похоже, требует десктоп Chrome/Edge.
- **Не описывать дизайн словами и не «выбирать один» за владельца** — давать несколько живых вариантов.

## Новые приоритеты (= задача новой сессии: инвентарь «что осталось ВЕЗДЕ»)
1. **Запуск сайта klimat-pro-AI в реальную работу** — что блокирует «настоящий» запуск (стабильность,
   мультиюзер, минорные баги m3/m4, бэклог, домен/TLS, надёжность туннеля/WSL-автозапуск). Источник: `docs/BACKLOG.md`,
   `deploy/INFRASTRUCTURE.md`, recheck-карта.
2. **ИИ-модуль + дизайн для компании владельца** — новый крупный пласт, не разложен. Что это (чат-бот?
   внутренний инструмент? модуль в дашборде?), для кого, какой функционал, какой UI. Нужен brainstorming.
3. **MCP, закрывающий и личное, и компанию** — какие MCP-серверы, под какие задачи, общие для личного
   сайта и компании. Связь с пунктами 1-2.
4. **Парковка-дизайн (низкий приоритет):** выбрать модалку (1-4) и шапку (1-3) из Claude Design →
   перенести в код (унификация `Modal`+`TaskModal` — `App.jsx:1397` и `:4645` сейчас две разные системы);
   плюс остаток спеки `docs/design-polish-2026-06-24.md` (мобайл-баланс, типографика/рукописный слоган, max-width).

## Открытые вопросы для владельца (в новой сессии)
- Что конкретно значит «выпустить сайт в работу» — какой критерий «готово»?
- «ИИ компании» — что это по сути? (определить субъект перед дизайном).
- MCP — под какие конкретные задачи, и что общего между личным и компанией?
- Порядок и важность трёх инициатив — разложить вместе.

## Ключевые пути/факты
- Репо: `F:\Сайт\redesign-v2-fresh`; remote `github.com/daniileliseev1337/klimat-pro-AI`.
- git push: обход прокси — `unset HTTPS_PROXY HTTP_PROXY` + `git -c http.proxy="" push`.
- Хаб-ПК (developer-marker есть) → push в main разрешён.
- Спека дизайна: `docs/design-polish-2026-06-24.md`. Бэклог: `docs/BACKLOG.md`.
- Лимит: владелец просит беречь недельный лимит — быть эффективным, cascade-loading, не жечь зря.
