# Сессия 2026-07-07 — Блок 4 реворка: гигиена журналов и индексов

## Что сделано (все пункты плана + накопленные кандидаты)

1. **Трекер (build_tracker.py + xlsx)**: формулы листа «Прогресс» приведены к фактическому
   словарю статусов (B4: несуществующее «К изучению*» → «Забыт*» = 14; B6 «ДОЛГ ИСПОЛНЕНИЯ»:
   несуществующий статус → COUNTIFS(Забыт× × Использовать*/Поддерживать*) = честный 0 —
   docling установлен, OCRmyPDF/camelot отклонены как перекрытые; B8: +wildcards и
   «Поддерживать» = 18). Охват «(38 из десятков тысяч)» → динамический len(rows) = 67.
   Ведущий «=» в заметке ID41 — перефразирован + generic-guard в генераторе (cell.data_type='s').
   ID40 MinerU → «Стоит (внедрён)» / «Использовать» (+актуальная заметка про MCP и нишу);
   ID67 → «Разобрано (частично)» (+ретайр banner/slides, pdf-edit). Легенда = реальный словарь.
   Перегенерирован; кэш формул восстановлен Excel COM; значения проверены data_only-чтением.
2. **skills.md → генератор** `scripts/build_skills_index.py` (решение владельца): PyYAML с
   ручным fallback, первый абзац description, 34 скилла, шапка «ГЕНЕРИРУЕТСЯ, руками не править».
3. **session-reports.md удалён** (решение владельца — мёртвый индекс).
4. **agents/_TEMPLATE.md → templates/agent_TEMPLATE.md**: харнесс регистрировал шаблон как
   спавнимого агента-плейсхолдера (побочная находка ревизии 02.07). Ссылки в 16 агентах +
   README обновлены; .gitignore whitelist +!templates/; история (session-reports/harvested/docs)
   не тронута по конвенции.
5. **aggregate-tool-usage.ps1**: +[Console]::OutputEncoding UTF-8 (WARN аудитора Блока 2 —
   кракозябры в OEM-терминалах); мёртвое исключение '_TEMPLATE.md' убрано.
6. **pull-feedback.ps1**: фильтр smoke-заглушек (updater-smoke-test и вариации) — мусор
   больше не возвращается в inbox при каждом pull.
7. **mineru_extract.py**: partial-save каждого чанка сразу (.partNN.md), resume при
   перезапуске (готовые чанки не отправляются в облако повторно — квота цела), 3 ретрая
   с паузами 30/60 с. Оффлайн-тест resume: PASS (сборка из кэша без сети, part-файлы
   убираются после успеха).
8. **feedback-inbox**: 45 → 28 файлов (12 smoke-заглушек + 5 старых копий k7-pyrevit-отчёта).
9. **statusLine ccusage убран из settings.shared.json** (решение владельца: «ccusage никто
   не использует»); личные statusLine merge не трогает.

## Попутные находки (сверх плана)

- **Баг раскраски трекера**: код читал вердикт из d[8] (колонка Tier) вместо d[7] —
  красная/зелёная заливка по вердикту не работала с рождения файла. Починен.
- **Путь-баг генератора**: outdir = ~/Desktop/... — на ноуте Desktop не редиректится в
  Я.Диск «Рабочий стол», трекер уезжал бы мимо папки программы. Теперь пишет рядом со скриптом.
- Опечатка-иероглифы в собственном комментарии поймана самопроверкой (инструмент может
  генерить смешанные скрипты — вычитывать вставки).

## Решения владельца в сессии

skills.md = генератор · session-reports.md = удалить · ccusage из shared = убрать
(один AskUserQuestion, все три — рекомендованные варианты).

## Проверки

Трекер: openpyxl data_only — 8/8 метрик с кэшем, значения сверены с python-истиной по AST;
генератор индекса — 34/34 с содержательными описаниями (spot-check стилей YAML `>-`/`|-`);
оба .ps1 — PSParser 0 ошибок + живой прогон aggregate; mineru — оффлайн resume-тест PASS;
shared JSON валиден, hooks целы.

**Ревьюер auditor: PASSED 10/10.** Первую выдачу вердикта оборвало сетью (те же проблемы
соединения, что днём) — лечение по памяти: SendMessage тому же агенту, не перезапуск;
он честно выдал промежуточный NOT PASSED («не всё проверено»), затем допроверил 5 хвостов
живыми прогонами: свой оффлайн-тест resume mineru, живой aggregate, корректный PSParser
(поймал, что первая попытка через bash-интерполяцию проверяла пустую строку), пересчёт
inbox=28, git-diff «ровно 1 строка × 16 агентов», CLAUDE.md нетронут. WARN: excel-MCP
read_data_from_excel отдаёт текст формул (нет data_only) — кэш xlsx смотреть openpyxl.

## Итог для программы

**План фиксов ревизии 02.07 закрыт целиком (Блоки 0–4).** Открытым по документации остался
только diff CLAUDE.md:134 (строка «Хуки» — дополнить project-memory), ждёт «ок» владельца.

## Уроки

- Формулы отчётных листов сверять с фактическим словарём данных программно (AST → Counter →
  COUNTIF-семантика), иначе метрики молча нули.
- «Мёртвый ноль» ≠ сломанная метрика: ДОЛГ=0 оказался правдой — метрика остаётся трипваером.
- Пути вида ~/Desktop не переносимы между машинами с разными редиректами — артефакты
  программы писать относительно самого скрипта.
