# Сессия 2026-06-05 — ОВ-проект (приток), узел установки + смена метода трассировки

## Что сделал
- **Узел приточной установки пересобран правильно, БЛОКАМИ** (был забракован как неверный):
  патрубок → ø200 круг → **блок шумоглушителя** («шумоглушитель 200х600») → переход круг→прямоуг (трапеция вручную, блока круг→прямоуг в проекте нет) → прямоуголка → **риса W300** → 45° W300 → коллектор. Сверено наложением осевых на красную трассу PDF (зелёное на красном).
- **Корневая находка:** главный ствол = **300×100 (в плане 300)**, а не 140 (память была ошибочна; площадь ø200 ≈ 300×100). Это была причина «не совпадает с PDF» у установки.
- ø160 ветка от коллектора к зоне 13/14 + терминал #14 (пленум+спуск+решётка) как эталон техники.
- **Инвентарь блоков пополнен:** есть «шумоглушитель 200х600», «Шумоглушитель 180», Переход_гибкий (круг↔круг), Редукция, Отвод 90/45(+гибкие), Тройник/Тройник_В, Диффузор, Воздуховод_гибкий (dyn ø). Блока круг→прямоуг нет.

## Где сломалось / уроки
- **Зависание AutoCAD** на длинной правке: модальный диалог/команда блокирует file_ipc (даже `(+ 1 2)` таймаут; `status/health` живут — их отдаёт сервер). Пользователь закрыл/открыл AutoCAD → **несохранённое потеряно**. Лечение: `system init` (подхватывает новое окно, hwnd меняется) + заново `(load toolkit)` + переопределить helper'ы. **СОХРАНЯТЬСЯ ПОСЛЕ КАЖДОГО УЗЛА.**
- **Не морозить подложку** — морозил для чистых рендеров, пользователь увидел пустой экран → «рисуешь вслепую, не сверяешь». Подложку держать видимой всегда.
- **`vla-getboundingbox` в больших progn** → «неверно сформированный список»; оборачивать в vl-catch или избегать.
- `PNGOUT` не рендерит растровые подложки.
- **Метод пиксельной привязки 2 схем забракован** (reshetki vs duct-схема дают позиции решёток с разбросом ~650мм — не та точность).

## Смена метода (идея пользователя, внедрено)
Растровую схему PDF с прозрачным фоном (`scheme_trace.png`) наложить ПОВЕРХ подложки в DWG (привязка sch2model, IMAGEATTACH, V-вектор 2.2304, transparency on, draworder back, handle 40FCCD) → обрисовывать остаток прямо в AutoCAD поверх неё. Видно сразу совпадение, не возвращаясь к PDF, расхождение схем уходит (один источник). Детали и состояние — память `project_proektirovanie_ov_dwg_recreation` блок #6.

## Состояние
Файл `C:\temp\Mosfilm_Y703_OV.dwg` сохранён. Картинка-обтравка привязана. Пользователь продолжит дома обрисовкой поверх обтравки: верх коллектора, 2 гребёнки+спуски+7 решёток, пом.16, левые комнаты, коридоры, фитинги блоками.

## Обезличивание
Объект — `<объект-Москва-ЗАО>`, без ФИО/шифров/адресов.
