# Session report — daniil-dashboard: волна фиксов + переработка видимости (2026-06-11)

Полный persistent-статус проекта — в auto-memory `project_daniil_dashboard_v3.md`
(загружается каждой сессией). Здесь — срез именно этой сессии.

## TL;DR
- Закрыли баг доходов на дашборде, прогнали полный аудит сайта (Fable 5) и реализовали
  большинство его находок, плюс новые замечания владельца по ходу.
- Кульминация — переработка модели видимости проектов (RLS) + команда при создании.
- Всё задеплоено на прод и проверено владельцем. `origin/main` = `ea92716`.

## Что сделано (хронология коммитов, все на проде)
- `c8ba0a9` — баг «доходы=0»: `projectIncomeTxs` вливает проектные платежи в Dashboard+Analytics.
- Аудит всего сайта (Fable 5, полное чтение кода) → `docs/superpowers/audit/2026-06-11-fable-site-audit.md`.
- `b6afabb`/`fe7abc0` — пакет аудит-фиксов: доля участника на карточке (C1), edge-уведомления
  комментариев проекта (C2) и ТЗ-задач (C3), запрет «угона» owner_id (C4), портал панели
  уведомлений против sticky-шапки (M1), approved-фильтр push (m1), устойчивость загрузки (m2),
  моб-секция платежей (m8), search_path (m4), чистка старых уведомлений (m5).
- `5ee843e` — секция «Платежи» в форме на всю ширину (схлопывалась).
- `3a37d77` — KPI «Портфель» с подписью «моё» (B) + сортировка проектов (№4).
- `c093f78` — разведены цвета пирогов «Финансы».
- `20a1d3d`/`d9eaa5b` — «глаз видимости» проекта (№9, поимённо кто видит) + бренд КЛИМАТ-ПРО в отчёте.
- `aa507c9`/`f06b8fa` — **новая модель видимости** (RLS): Личный=только владелец(+админ),
  Командный=владелец+команда, Маркетплейс=все; админ-чип «Все проекты»; команда при создании (№7);
  синхронизация «глаза» с новой моделью.
- `ea92716` — исполнитель→авто-член команды editor (C); бэкап включает платежи и доли (C5).

## Ключевые решения этой сессии
- **Модель видимости** (спек `docs/superpowers/specs/2026-06-11-visibility-model-redesign.md`):
  команда видит проект только при видимости «Командный»; админ — вариант A (видит всё через RLS,
  но UI прячет чужие личные, чип показывает). Миграция `20260611_0002_visibility_model.sql` применена.
- **№7 команда при создании** — секция «Команда» видна всегда (выбор владельца), при создании
  собирается черновик и применяется после insert.

## Открытые «хвосты» (для нового чата — детали в памяти проекта)
- **Побочка видимости:** у владельца несколько «Личных» проектов с назначенной командой —
  команда их больше не видит; при необходимости переключить на «Командный».
- **M2 путь 2:** исполнитель, берущий проект с маркетплейса сам, не добавляется в команду
  (RLS write owner-only) — нужен SECURITY DEFINER RPC.
- **C5 риск:** восстановление платежей/долей мапит старый→новый id проекта по позиции bulk-insert.

## Что осталось в очереди (всё требует владельца — дизайн/уточнение)
- G импорт CSV (крупная, brainstorm: устойчивый парсер vs LLM-edge).
- №10 история действий (БД-логирование, дизайн); №11 убрать стадии (уточнить какие).
- D роль «заказчик» (brainstorm); редизайны #2 TasksView и #4 ReportViewer+отчёт по выбранным
  (в #4 закрыть XSS-innerHTML).

## Грабли среды (подтверждены этой сессией)
- Диск F: сбоит на fsync → git через `-c core.fsyncMethod=writeout-only` + ретраи; Edit даёт EIO,
  но запись применяется (проверять Read).
- Прод (живая БД / edge / веб-деплой / push) блокируется auto-классификатором без слова «деплой».
- Деплой БД-миграций: `docker exec -i supabase-db psql < файл` через wsl; веб — `deploy-web.sh`;
  edge — копировать index.ts в `volumes/functions/<fn>/` + `docker restart supabase-edge-functions`.
- После каждого деплоя владельцу нужен сброс кэша PWA (новый бандл иначе не подхватывается).
