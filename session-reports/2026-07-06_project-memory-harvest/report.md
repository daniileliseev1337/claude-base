# Отчёт сессии 2026-07-06 — harvest + дизайн скилла project-memory (handoff)

> Сессия на DANIIL-LAPTOP. Охват: восстановление контекста этажа-2 → harvest готовых решений
> project-memory → brainstorming → одобренная спека. Реализация — в НОВОМ чате (handoff FULL).

## TL;DR
- Восстановлен контекст «что делали на домашнем ПК»: этаж-2 закрыл оба хвоста (reconcile-фикс
  «вне зоны пола» + manager_parallel dry-run); переносимые тесты прогнаны здесь: 17/17 + 14/14 PASS.
- Harvest: ниша «файловая память проекта для Claude Code» — конвергентный стандарт (10+ репо);
  разобраны целиком claude-cortex и claude-context-os (оба MIT, клоны в `C:\hv`).
- Brainstorming: дизайн скилла `project-memory` одобрен владельцем; спека написана.
- Следующий шаг (новый чат): superpowers:writing-plans по спеке → реализация → тесты + auditor.

## Состояние артефактов
- **Спека (источник истины):** `<папка программы>\docs\superpowers\specs\2026-07-06-project-memory-skill-design.md`
  (папка программы = «Заведомо проигранный бой (или нет)» на рабочем столе Claude).
- **Harvest-сводка:** `harvested/SUMMARY.md` рядом с этим отчётом (таблицы кандидатов, классы, вывод).
- **Клоны референсов:** `C:\hv\claude-cortex`, `C:\hv\claude-context-os` (MIT; временная папка вне Я.Диска).
- **Память обновлена:** `etazh2_poc_done_2026-07-03.md` (хвосты закрыты, копия в базе отстала) + MEMORY.md.
- Git: проект без коммитов (репо пустое), ничего не коммитилось (правило + Я.Диск).

## Ключевые решения владельца (НЕ переспрашивать в новом чате)
1. Направление: СВОЙ скилл, не чужой фреймворк целиком (у пользователя зрелая ручная схема — эталонный
   боевой объект: `Claude/` с CLAUDE.md-стартовым, README-навигатором, ЖУРНАЛ СЕССИЙ.md, статус-файлами).
2. Подход A: один фокус-скилл `project-memory` (bootstrap + rot-курирование), код в `tools/`, **хук обязателен**.
3. Ядро + профили: v1 — только нейтральное ядро; профиль `id-tom` НЕ делать (пустая точка расширения
   `templates/profiles/`); первым обкатывает блок ПТО.
4. Хуки событийные, БЕЗ порогов в днях (всё делается в пределах дня): SessionStart — печать верха журнала;
   SessionEnd/Stop — «файлы менялись, журнал нет → сессия не закрыта».
5. Без git-на-памяти: мультидевайс = Я.Диск + журнал-координатор + относительные пути; откат apply — `_backup_<дата>`.

## Где сломался / уроки (транспорт и окружение)
- `gh` CLI на ноуте НЕТ; egress иностранный (NL/Cloudflare).
- `curl --noproxy "*"` до api.github.com НЕ проходит; через прокси проходит, но анонимный rate-limit
  60/ч быстро кончается → **метаданные GitHub брать через Exa** (`web_fetch_exa` на api.github.com,
  maxCharacters ≥ 5500, иначе licence обрезается).
- `git clone` падал дважды: (1) унаследованный `GIT_DIR` из окружения — лечится `unset GIT_DIR
  GIT_WORK_TREE GIT_INDEX_FILE`; (2) MAX_PATH в длинном scratchpad-пути — клонировать в короткий
  (`C:\hv`).
- PowerShell/py print с эмодзи → 'charmap' — ставить `PYTHONIOENCODING=utf-8` (известная грабля).
- Звёзды — слабый сигнал в свежей нише (все файловые шаблоны — весна 2026, 9–47⭐): отбирать по структуре.

## Открытые вопросы (для нового чата, в плане)
- Спека §10: установка хуков (сниппет settings.json vs update-config); поток apply
  (Python готовит diff → Claude ведёт апрув через AskUserQuestion); нужен ли commands/-файл;
  механика session_end-хука (ивент + метка времени старта).

## Хвосты ВНЕ темы (не трогать без запроса, зафиксированы в памяти)
- Этаж-2: живой параллельный батч — только после approve владельца; базовая копия
  `skills/supervisor/tools/etazh2_judge/` отстала от проекта (без reconcile-фикса) — синкнуть при
  следующей приёмке.
- Реворк базы (HANDOFF от 24.06 в папке программы) — отдельная ветка, не трогалась.

## Промпт для нового чата

> Привет. Это продолжение сессии «скилл project-memory» (handoff 2026-07-06, DANIIL-LAPTOP).
>
> Контекст: harvest и brainstorming пройдены, дизайн ОДОБРЕН владельцем. Спека — единственный
> источник истины: `…\Рабочий стол\Claude\Заведомо проигранный бой (или нет)\docs\superpowers\specs\2026-07-06-project-memory-skill-design.md` — прочитай её ЦЕЛИКОМ первой.
>
> Решения владельца (приняты, не переспрашивать): v1 = нейтральное ядро (bootstrap.py) +
> rot-курирование (curate_rot.py: propose→review→apply с `_backup_<дата>`) + хуки ОБЯЗАТЕЛЬНЫ
> (событийные, БЕЗ порогов в днях); профиль id-tom НЕ делать (пустая точка расширения — первым
> обкатает блок ПТО); git-на-памяти НЕ использовать (Я.Диск + журнал + относительные пути).
>
> Что делать: 1) superpowers:writing-plans по спеке → план реализации; 2) реализация в
> `~/.claude/skills/project-memory/` (код в tools/, шаблоны обезличенные, тесты переносимые);
> 3) приёмка: тесты + ревьюер auditor (обезличивание шаблонов) перед выдачей; 4) открытые
> вопросы спеки §10 закрыть в плане, неоднозначное — вопросом владельцу.
>
> Справочно (по необходимости, cascade): harvest-сводка
> `~/.claude/session-reports/2026-07-06_project-memory-harvest/harvested/SUMMARY.md`; клоны
> референсов `C:\hv\claude-cortex` и `C:\hv\claude-context-os` (MIT, идеи оттуда уже в спеке).
>
> Перед началом — STOP-процедура CLAUDE.md ПОЛНОСТЬЮ (MCP/agents, роутинг-гейт). Вводные полные,
> грилинг не нужен. Хвосты вне темы (этаж-2 батч/синк) — не трогать.
