# Session report — граф устройства тома ИД (skill id-volume-graph) + привязка id-engineer

**Дата:** 2026-06-08 · **Хост:** DANIILPC (hub, MAX x5, отдел Проектирования) · **Роль:** developer (маркер есть)

## TL;DR
- Построен **queryable + редактируемый граф устройства тома ИД** (методология, обезличено) в формате graphify: **46 узлов / 89 рёбер / 5 гиперрёбер / 7 сообществ**.
- Оформлен как **скилл `id-volume-graph`** в claude-base (graphify-out + редактируемый исходник + tools-обёртки query/rebuild), **id-engineer привязан** (query графа вместо «простыни»).
- **auditor: PASSED** — фиделити модели, без противоречий/выдумки, **0 PII** по 66 паттернам (хотя первоисточник насквозь в ФИО/шифрах/артикулах).
- Развилка метода решена с пользователем: **подход A2** (синтез графа в облаке) на **DANIILPC** — Ollama/GPU не нужны.

## Что сделано (по фазам)
1. **Изучение корпуса** (полно): 3 session-report + INDEX + 18 feedback-файлов (`work/2026-06-08_id-session/`) + методичка `Обучение составления ИД.md` (разд. 1–26) + карта эталонного тома + реестры замечаний/журнал каскада/инвентарь.
2. **Сверка понимания** (по требованию пользователя): 54 утверждения + 2 раунда уточнений → запечатанная модель. Вскрылись правки прошлого Claude:
   - **методичка §26 ОШИБОЧНА**: «один набор ИС шарится между актами» — неверно; ИС 1:1 со своим актом, у АОУСИТО свои «общие схемы». Граф отражает исправленную модель.
   - **ПВ — master**, ВСО/ВОР — её проекции; инвариант **ПВ ≡ ВСО ≡ ВОР ≡ ИС**; цепочка происхождения Спецификация РД → ПВ → {ВСО,ВОР}.
   - **подсчёт позиций — по PDF, метод НЕ финализирован** (прошлый вывод «DWG-COM = канон» снят).
   - порог Реестра №X = перелив 2-страничной вёрстки акта; паспорт — по числу PDF-страниц; Data Link — ушли (не абсолют); гранулярность актов переменная.
3. **Сборка графа:** курированный extraction-JSON → `graphify build` (build_from_json + cluster + to_json) → graph.json + HTML + GRAPH_REPORT.
4. **Проверка:** query/path/explain — оффлайн, cwd-независимо (через tools-обёртку).
5. **Ревью + размещение:** auditor PASSED → скилл в claude-base, id-engineer привязан, индекс skills.md обновлён.

## Ключевая находка (инфраструктура)
- **graphify штатно использует Claude Code субагентов** как LLM семантического извлечения (или Gemini опц.). **Ollama в SKILL.md не фигурирует.** Прошлый вывод «docs-граф упёрся в Ollama/16 ГБ» был тупиком именно Ollama-эксперимента — штатный путь hardware-free. Для нашего кейса извлечение делал я (облако), query готового графа — оффлайн (0 токенов LLM).

## Артефакты
- Скилл: `~/.claude/skills/id-volume-graph/` — `SKILL.md`, `graphify-out/{graph.json, graph.html, GRAPH_REPORT.md}`, `id_volume_extract.json` (редактируемый), `tools/{id_graph.py, rebuild.py}`.
- Правки: `agents/id-engineer.md` (Required reading + Related + changelog), `skills/skills.md` (индекс).
- Рабочая (заигнорено, необезличено): `~/.claude/work/2026-06-08_id-session/graph/` — build/finalize-скрипты.

## Где «сломался» / уроки
- **explain/path берут точную метку узла** — спецсимвол `≡` в метке инварианта ломает матч; `query` (подстрока+IDF) устойчив → он основной интерфейс (зафиксировано в SKILL.md).
- **Не доверять артефактам прошлого Claude как эталону без сверки**: методичка §26 и feedback (DWG-COM) содержали ошибки/неокончательные выводы, вскрытые только грилингом пользователя. Verification gate перед построением себя оправдал.
- **graphify hand-authoring:** пишем extraction-формат (nodes/edges/hyperedges), прогоняем build → graph.json; кастомные RU-relations сохраняются как атрибуты рёбер, query/path/explain работают.

## Открытые хвосты
- Граф пока **только ВиВ** — расширять на ОВ/ЭО/СС по мере работы (узлы/рёбра общие, инстанс-нюансы добавлять).
- Опц. «critic на полноту»: прогнать нативное graphify-извлечение по **обезличенной** копии корпуса — найти упущенные узлы/рёбра. Не делали (по согласованию — потом).
- Метод подсчёта позиций по чертежу — отдельная задача (возможно свой под раздел).
- Корпус в `work/` необезличен — при любом переносе в claude-base обезличивать (граф уже чист).
