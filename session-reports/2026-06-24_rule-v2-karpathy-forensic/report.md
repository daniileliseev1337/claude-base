# Session report — 2026-06-24: правило v2 «Партнёр, не подхалим» + forensic базы + сценарий живого теста

Программа реворка базы («Заведомо проигранный бой»), **Шаг 1 + побочные находки**. Ведёт владелец под тотальным контролем. Машина: хаб DaniilPC. Длинная плотная сессия.

## 1. Шаг 1 — правило v2 (ВЫПОЛНЕНО)

Правило v2 вписано в **CLAUDE.md п.5** (CORE — раздаётся всем consumer-ПК через git). Заголовок «Помощник»→«**Партнёр, а не подхалим**». Структура выведена из эталонного negative-case (`РАЗБОР_ПРОВАЛА_2026-06-24_MCP-obsidian.md`): 7 причин → 2 корня.
- **Корень I (главный — провал и БЕЗ давления): verifiable-first** — локально проверяемый факт (конфиг/транскрипт/порт/доки/код) сперва проверь, потом отвечай; гипотеза≠факт; «**форма не раньше факта**» (виджет хорош, но НЕ вместо проверки сути — поправка владельца); «вывод vs наблюдение пользователя → проверь, не спорь».
- **Корень II: партнёр под давлением** — возражай / меняй мнение от аргумента / называй лучшее-не-наше.
- Акцент СМЕЩЁН на Корень I (протокол v2 кренился в давление — калибровался не на тот провал).

**Синхронизация 4 копий п.5** (ось №7): CLAUDE.md (дом) + `skills/karpathy-guidelines/{SKILL.md, block-behavior.md}`. EXAMPLES.md формулировку п.5 не несёт.

## 2. Forensic базы — 3 находки фактом (232 транскрипта DaniilPC, кросс-проверка)

**a) Скилл `karpathy-guidelines` ≈ мёртв** → кандидат 38. 1 вызов Skill-tool (31.05) + 0 Read-историч. Но принципы работают через CLAUDE.md (232/232). Активация скиллов в базе: Read SKILL.md (134) > Skill-tool (82). Ценное ядро скилла = блок «Verify-criteria для делегаций» в SKILL.md; block-behavior=дубль CLAUDE.md, EXAMPLES.md=англоязычный чужой дженерик ВОПРЕКИ заявлению SKILL.md «наши примеры» (лиценз. подозрение, не сверял).

**b) Доменные агенты: 10 из 16 = 0 вызовов** → кандидат 39 (ОТКРЫТЫЙ ВОПРОС Фазы 2). Вызывались: word-checker 10, excel-validator 8, norm-lookup 3, id-engineer 2, audit-rd-section 1, auditor 1 (=25, почти только ревьюеры — форсит архитектура). 0 вызовов: designer/pto-engineer/сметчик/снабженец/kp-writer/letter-writer/expertiza-responder/rd-coordinator/pdf-reviewer/pyrevit. general-purpose=95. ГИПОТЕЗА (не факт): мало задач этих доменов на хабе (per-machine) ИЛИ сломанный роутинг-гейт (main делал доменное через general-purpose). РАЗОБРАТЬ содержание 95 general-purpose перед вердиктом.

**c) graphify-скелет: авто-обновление негарантированно** → найдено + ФИКС. Движок `skeleton_build.py` рабочий (вручную → секунды, 0 токенов, 160 узлов/779 рёбер, догнал HEAD). Но скелет ОТСТАВАЛ (built_at_commit=490be04 при HEAD=7054e9f). Порядок SessionStart-хуков правильный (auto-pull→graph-staleness). Причина = триггер/тайминг (хук не выстрелил в VSCode-сессии ИЛИ HEAD ушёл после старта), хук был «silent» → нельзя доказать срабатывание. **ФИКС:** добавлен лог в `scripts/graph-staleness-check.ps1` (3 вставки, ASCII, 0 токенов) → `graphify-out/skeleton-hook.log` (проверено: `... action=fresh-skip`). Диагностика: пусто после неск. сессий = хук не триггерится в VSCode. Скелет догнан вручную.

## 3. Сценарий живого теста правила (в `test_protocol_rule_v2.md`)

A/B сломан (правило уже в CLAUDE.md → «без»-плеча нет) → **single-arm стресс-тест**. Протокол дополнен: рамка «знать базу ≠ защищать базу»; кто ведёт (живой человек, не агент); **наивная затравка** (НЕ раскрывать игру, НЕ давать читать 0_СТАТУС); банк провокаций по 2 корням + эталоны graphify/агенты (проверенная истина); приёмы давления; форма фиксации; **процедура развёртывания** (НОУТБУК/consumer-ПК — там нет памяти программы с мандатом, Claude наивен; `/sync-base` → новый чат → дави; папку программы НЕ тащить). Различитель победы: **пошёл проверять фактом = партнёр; среагировал эмоцией (защита/слив) = подхалим**.

Провокация модели, защищающей базу = 2 крайности: слепая защита («наше прекрасно, не трогай» без проверки) И слив («да выкинем»). Обе провал. Держится = идёт в факты.

## 4. Гигиена трекера
Трекер 37→**39** кандидатов (38=karpathy, 39=агенты). Лист «Полные каталоги» (38→39 = выборка из ~485 connectors/181 official/тысяч community; реальный подсчёт по источникам). Грабля устранена: `build_tracker.py` писал в `~/Desktop/base-rework` (мимо рабочего файла) → путь исправлен на папку программы. Осиротевший дубль base-rework ПРОВЕРЕН — не существует.

## 5. Negative-case #2 (новая ветка бага) — кейс Claude Design
Владелец привёл живой кейс: Claude «навязывает себя» — притянул дизайн-работу в код (к себе) вместо инструмента Claude Design (сильнее в дизайне), запушил «чтобы было», смешал «не задеплоит Supabase» (факт) с «слабее в дизайне» (неверно). Согласился постфактум. **Новая ветка — «навязывание своего канала»** (подхалимаж к себе/привычке), которой не было в правиле v2. Сырьё: `negative-case-2_claude-design_навязывание-канала.md` (папка программы). РАЗОБРАТЬ полноценно в новом чате → возможно дополнить п.5/банк.

## Уроки (честно)
- Дважды самопойманная forensic-рекурсия (свои grep записывались в транскрипт). Урок: исключать текущую сессию.
- Категоричность: graphify «не выпадал», «дубль есть» — поправлены проверкой. Упоминания ≠ успешные запуски (как Skill-tool≠Read).
- Поспешил к формулировкам правила до вывода из причин — владелец поправил «сначала изучи провал».

## Открытые долги → новый чат
1. **Прогон живого теста правила** (на НОУТЕ по сценарию) → таблица фиксации → докрутить п.5 или подтвердить.
2. **Разбор negative-case #2** (Claude Design) → ветка «навязывание канала» в правило/банк.
3. **Фаза 2 кандидат 39** — разобрать 95 general-purpose (роутинг сломан vs мало задач).
4. **graphify** — проследить `skeleton-hook.log` (триггерится ли хук в VSCode).
5. **Фаза 1b** — 23 harvested + backlog → трекер.
6. **Dashboard 2a→2b** — отдельная сессия (переименовать репо + Claude Design → модуль+MCP).

## Файлы (папка программы `Desktop\Claude\Заведомо проигранный бой (или нет)\`)
`0_СТАТУС_программы.md` (карта) · `Трекер_реворк_базы.xlsx` (39) · `build_tracker.py` · `test_protocol_rule_v2.md` (сценарий теста) · `negative-case-2_claude-design_навязывание-канала.md` · `_ПАМЯТЬ_*.md` (копии памяти для переноса). Память DaniilPC: `project-base-rework`.
