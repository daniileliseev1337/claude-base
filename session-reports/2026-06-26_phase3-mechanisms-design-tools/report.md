# Session report — 2026-06-26 — Фаза 3 (механизмы-хуки) + дизайн-инструменты

Программа реворка базы. Длинная сессия: телеметрия → роутинг-хук → установка UI/Magic → жёсткий грилинг → фикс handoff. Всё запушено в claude-base.

## Сделано (по порядку)

1. **Телеметрия tool-usage — выяснил, что УЖЕ работает** (опроверг устаревший диагноз памяти «не зарегистрирован нигде»). PostToolUse-хук `log-tool-usage.ps1` есть в settings.shared.json + settings.json, пишет `~/.claude/.local-state/tool-usage.jsonl` в реальном времени. Обновил память программы (0_СТАТУС + _ПАМЯТЬ). Запушил коммит на удалёнку (был unpushed из-за закрытия прошлой сессии через /clear — НЕ прокси). Урок: статус инструмента судить запуском/проверкой файла, не записью памяти (устаревает за часы).

2. **Роутинг-детектор хук** (`scripts/routing-detector.ps1` + `routing-reminder.txt`) — коммит `ede77e3`. Форсит роутинг-гейт на доменных задачах (лечит general-purpose > все доменные). Паттерн grilling-detector, переиспользует общий `domain-grilling/triggers.txt` (унификация). Подключён в UserPromptSubmit (shared+local). Тест hit/miss — ок. Активируется со следующего старта.

3. **Установка дизайн-инструментов (локально, НЕ в базу) для Claude Design:**
   - **UI UX Pro Max** — комплект 7 скиллов (ui-ux-pro-max + design/design-system/ui-styling/brand/slides/banner-design), 84 стиля/161 палитра/73 пары шрифтов. Скопирован в `skills/`, изолирован gitignore-блоком (проверено `git check-ignore` — в базу не уходит). Подхватился без перезапуска.
   - **Magic MCP** (21st.dev) — `claude mcp add magic -s user` (в `~/.claude.json`, вне base-репо). Status Connected. Ключ API локально, в базу не попал (проверено `git grep`). Tools видны после перезапуска Claude Code.

4. **Жёсткий грилинг (opt-out)** — коммит `856bc3d`. Инверсия: вместо «предложи выбор» (opt-in) → грилинг по умолчанию ВКЛ при неполных вводных, пользователь отменяет явной фразой. Согласовано в 3 местах: `reminder.txt` (хук-впрыск), `SKILL.md` (5 правок), CLAUDE.md секция. Решение владельца. Тест hit/miss — ок.

5. **Фикс handoff** — коммит `49008c6`. LITE-промпт велел «STOP-процедуру выполни кратко» (двусмысленно → читалось как «не тщательно»). Разделено: ПОЛНОСТЬЮ (сверки/гейты) + отчёт одной строкой. В оба шаблона (FULL+LITE) вшит вызов domain-grilling для доменных next-step. Наблюдение владельца.

## Решения владельца
- Грилинг — **жёсткий** (opt-out).
- Виджеты как мост Claude↔пользователь — **через HTML-файл** (Claude генерит, пользователь открывает в браузере/VS Code; в десктопе строит прямо в чате). Среда рендера — не голый CLI, а HTML.
- Workflow #12 — ослабить (согласен, аргумент про токены верный) — ОТЛОЖЕНО.
- Веб-секцию CLAUDE.md НЕ выносить в memory (критична в момент первого захода — аргумент владельца принят, моё предложение имело дыру).

## Идеи в работе (для следующей сессии)
- **ВИДЖЕТ-ПРОТОТИП (приоритет):** после грилинга Claude строит виджет «как понял задачу + план» в HTML. Konva vs Fabric (владелец анализировал) vs чистый HTML/SVG. Объединяет: усиление грилинга + наглядность + один спектр пользователь/Claude.
- **harvest-переработка** — владелец назовёт симптом, проверить фактом.
- **Workflow #12 ослабить** — срезать проактивное предложение, детали в memory/reference_workflow_tool (уже там), оставить deterministic-first.
- **Гео-хук веб** — SessionStart определяет egress машины → впрыскивает состояние (a/b/c). Усиливает веб-надёжность.

## Уроки
- Статус инструмента судить ЗАПУСКОМ, не записью памяти (повтор урока graphify).
- `/clear` не триггерит SessionEnd → ни auto-push, ни session-report. Перед /clear пушить и сохранять вручную.
- Веб-знание нужно в момент первого захода → не выносить в memory-указатель (там грузится по решению модели ~70%, поздно).
- При длинном кириллическом home — временные файлы в коротком ASCII-пути (MAX_PATH, short-name `88F3~1` нестабилен для Read).

## Коммиты (origin/main)
- `ede77e3` routing-detector hook
- `856bc3d` domain-grilling жёсткий (opt-out)
- `49008c6` handoff фикс + grill-me
- gitignore-изоляция дизайн-скиллов — в `2383b39` (auto-sync)
