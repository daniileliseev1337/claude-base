# Handoff: открытые вопросы ПТО на Даниила (vault stroydok)

Закрытие сессии с DANIILPC. Модель: Opus 4.8 (1M). Канал работы с vault — **MCP Obsidian** (mcp-obsidian), ФС только где у MCP нет аналога.

## TL;DR

- Работали в vault'е `C:\Users\Даниил\Documents\Obsidian Vault` (проект **stroydok**, customer zero — Даниил).
- За сессию: закрепили MCP Obsidian как дефолт, завели первую запись Даниила в учёте часов (W19), ответили и закрыли вопрос ТЗ MVP v2.2, разобрали открытые вопросы.
- **Осталось:** ответить на 5–6 открытых вопросов на Даниила. Идея новой сессии — проверить, сможет ли личная ПТО-база (доменные агенты `id-engineer`/`pto-engineer` + память по ИД) верно ответить на доменные вопросы. Ответы валидируют базу И закрывают вопросы.

## Цель новой сессии (главное)

Даниил хочет **проверить личную ПТО-разработку**: на этом ПК Claude натренирован как практик ПТО (доменные агенты, накопленная память/граф по ИД, ПВ, ВОР, томам). Гипотеза: на доменные вопросы из списка ниже Claude/агенты должны ответить из накопленной базы — и если ответ совпадёт с реальной практикой Даниила, это:
1. закрывает открытый вопрос (ответ = ответ customer zero);
2. валидирует, что личная ПТО-база рабочая.

**Механика для нового чата:** по каждому доменному вопросу — сначала спросить ПТО-базу (доменный агент `id-engineer` через Task, или сверка с памятью/графом ИД: skill `id-volume-graph`, memory `id_volume_graph_built`), показать Даниилу что предлагает база, он подтверждает/корректирует. НЕ выдумывать практику — база отвечает, человек верифицирует.

## Что сделано в этой сессии

1. **MCP Obsidian как дефолтный канал.** Memory: `~/.claude/projects/C--Users-------/memory/feedback_mcp_obsidian_default.md` + строка в MEMORY.md. Маппинг tools (get_file_contents/list_files_in_dir/simple_search/append_content/patch_content), ФС-фоллбэк только где у MCP нет аналога (full rewrite, сложный regex, не-md, shell).
   - **Грабли MCP (важно):** `obsidian_patch_content` с `target_type=heading` падает `Error 40080 invalid-target` на русских заголовках с em-dash (—). `append_content` и `patch_content target=frontmatter` — работают. Вывод: правки по русским heading'ам — через ФС Edit, не через MCP heading-patch.
2. **Учёт часов — первая запись Даниила.** Файл `90 - Учёт работы/Часы по неделям/2026-W19.md`: суббота 2026-05-09, ~3.5 ч (чтение ТЗ MVP v2 1.5ч + интервью круг 3 с Кириллом 1.5ч + сессия с Клодом по vault 0.5ч). Сводка-строка Даниила обновлена с 0 → ~3.5. Правила учёта — `90 - Учёт работы/README.md` (часы = human-equivalent).
3. **Закрыт вопрос ТЗ MVP v2.2.** `60 - Текущая работа/Открытые вопросы/Уточнение формулировок ТЗ MVP v2.2 с Даниилом.md` — append-секция `## Ответ от daniil — 2026-05-10`, вердикт «вариант 2 — OK с корректировками», 6 ответов:
   - Q1 статусы ПВ: расширить (переезд в подраздел / учёт под другим шифром / в процессе уточнения);
   - Q2 ВОР в 2.7 OK + добавить процессную последовательность технадзора (схема → объёмы → ВОР → реестр);
   - Q3 ВОР универсальна для любого акта (АОСР/АОМР/АПП — примеры);
   - Q4 автоклон спецификации РД → ПВ обязателен в пилоте;
   - Q5 экспорт «было/стало» для закупок — в MVP;
   - Q6 терминология «предъявительская» (через ъ) — верна.
   Вопрос закрыт (status: closed, closed-at: 2026-05-10).

## Баг данных (исправить)

`assignee` Даниила пишется ДВУМЯ способами: `daniil` (3 старых ТЗ-вопроса, все closed) и `daniel` (новая пачка SD-вопросов). Один человек — Даниил. Dataview-фильтры их разделяют. Унифицировать (выбрать один вариант, прогнать заменой по всем нотам в `60 - Текущая работа/Открытые вопросы/`). По UI-формам в новых нотах используется meta-bind `inlineSelect` с option `daniel` — вероятно унифицировать к `daniel` или к `daniil`, согласовать с Кириллом.

## Открытые вопросы на Даниила (6)

Из 6 реально практики ПТО требуют не все — авторы (Кирилл) в блоке «Как отвечать» сами размечают продукт vs практику. Формат ответа во всех новых нотах: отметить `[x]` нужный чекбокс + добавить блок `### YYYY-MM-DD — Даниил` в секцию «## Обсуждение»; при закрытии — `status: closed`, `closed-at`, секция `## Резолюция`.

### Требуют практики Даниила (приоритет — это и есть тест ПТО-базы)

1. **SD-116 «Перенумерация выпущенного акта — семантика»** (`...Перенумерация выпущенного акта — семантика (SD-116).md`). ⚠️ блокирует старт бэка.
   - Q1: зачем реально перенумеровывают уже заведённый акт? (вставка пропущенного задним числом / опечатка / перенос в другой раздел РД → меняется шифр).
   - Q2: что разрешено менять (порядковый № / шифр раздела / полностью ручной № строкой).
   - Q3: одиночная или каскадная перенумерация (сдвиг хвоста 16→17→18).
   - Q4 (главное от Даниила): можно перенумеровывать **подписанный** акт или только Draft? По ADR-033 истина подписанного — скан, где № уже физически стоит → риск рассинхрона бумаги и системы.
2. **SD-100 «Том ИД — …мандатори ВОР/ВСО» Q3** (`...Том ИД — оценка страниц, копия на период, мандатори ВОР-ВСО (SD-100).md`).
   - Когда **ВСО** обязательна для акта, когда нет? Для каких актов мандаторна.
   - Флагать ли pre-flight'ом «на схеме совсем нет ВОР» как warning, или это ответственность ПТО (как сейчас).
   - (Q1 оценка страниц, Q2 копия тома на период — продукт, Кирилл.)
3. **SD-102 «Раздел РД — проектная документация и добивки»** (`...Раздел РД — проектная документация и добивки (SD-102).md`).
   - Q1: нужна ли в пилоте проектная документация раздела (ТЗ / проект / заключение экспертизы как вложения)?
   - Q2: «Поз.» РД отдельным номером от номера позиции ПВ? Нужна датированная редакция («ред.3 от 12.03.2026»)? Экспорт спецификации РД в Excel — на бэке или клиентом?

### Продуктово-архитектурные (решает Кирилл/Артём; от Даниила — максимум мнение customer zero)

4. **SD-98 «Список актов — паспорт акта и колонки экспорта реестра»** — Q1/Q2 продукт (Кирилл). От Даниила нужен только образец реестра/паспортов если есть. Кандидат на переназначение assignee → kirill.
5. **SD-105 «Паспортный сейф — истекает vs истёк»** — Q1 (сервер vs фронт разнесение состояния) — чисто архитектура Артёма, НЕ зона Даниила (по правилам vault Даниил архитектуру не ревьюит). От Даниила — лишь подтвердить, что различать «истекает» и «истёк» нужно (очевидно да). Кандидат → artem/kirill.

### Фактически закрыт

6. **«Нумерация позиций ПВ при вставке между — практика Даниила»** (assignee `kirill`) — Даниил уже ответил 14.06 через ТГ: «просто сдвигается вся ПВ» (сквозная перенумерация, `[x]` уже стоит). Висит продуктовый хвост на Кирилле (делает ли приложение автосдвиг само или инженер руками). От Даниила ввод по сути получен.

## Незаписанные часы (для учёта в W-файле)

- **Круг 4 интервью** с Кириллом (ПВ/ВОР, привёл к ТЗ MVP v2.2) — не зафиксирован в часах Даниила.
- **Эта сессия** (ответ на v2.2, разбор открытых вопросов, handoff) — добавить.
- Определить, в какой ISO-неделе фиксировать (зависит от реальной даты круга 4), дописать в `90 - Учёт работы/Часы по неделям/<W>.md`, раздел Даниила, по образцу записи за 2026-05-09.

## Конвенции vault (напоминание для нового чата)

- Работа с .md — через MCP Obsidian. Грабли heading-patch на кириллице — см. выше.
- Flow записи: черновик в чате → подтверждение Даниила → запись. Без подтверждения в vault не писать.
- Frontmatter: created/updated/status/owner/last-verified/tags; updated бампать при правке.
- Даты ISO YYYY-MM-DD. Содержание на русском.
- Не выдумывать практику ПТО — спрашивать базу/агентов и верифицировать у Даниила.

## Промпт для нового чата

> Привет. Это продолжение сессии «открытые вопросы ПТО на Даниила» с DANIILPC, vault stroydok (`C:\Users\Даниил\Documents\Obsidian Vault`).
>
> **Цель:** проверить мою личную ПТО-базу. На этом ПК я (Claude) натренирован как практик ПТО — есть доменные агенты (`id-engineer`, `pto-engineer`) и память/граф по исполнительной документации (ИД, ПВ, ВОР, тома). Хочу проверить: сможет ли база верно ответить на доменные вопросы из открытых вопросов vault'а. Если ответ базы совпадёт с моей реальной практикой — он закрывает вопрос и валидирует базу.
>
> **Что делать:**
> 1. Выполни STOP-процедуру CLAUDE.md (MCP-аудит, агенты, подтверждение). Работай с vault через **MCP Obsidian**.
> 2. Прочитай мой handoff-отчёт: `~/.claude/session-reports/2026-06-20_pto-open-questions-daniil/report.md` (НЕ читать целиком сразу — cascade: Grep "^##", потом нужные секции).
> 3. Возьми вопросы, требующие практики ПТО (в отчёте — секция «Требуют практики Даниила»): **SD-116** (Q1, Q4 — перенумерация акта, можно ли трогать подписанный), **SD-100 Q3** (когда ВОР/ВСО обязательны), **SD-102** (проектная документация раздела РД, добивки «Поз.»/дата редакции).
> 4. По КАЖДОМУ — сначала спроси ПТО-базу: подними доменного агента `id-engineer` (Task) и/или сверься с памятью/графом ИД (skill `id-volume-graph`, memory `id_volume_graph_built`). Покажи мне, что предлагает база как ответ.
> 5. Я подтверждаю или корректирую. После подтверждения оформи ответ в ноте: `[x]` нужный чекбокс + блок `### <дата> — Даниил` в секции «Обсуждение»; flow черновик→подтверждение→запись, без подтверждения в vault не писать.
>
> **Контекст и детали** — в отчёте: 6 открытых вопросов с разбивкой, баг assignee daniil/daniel, грабли MCP heading-patch на кириллице, незаписанные часы.
>
> Начни с SD-116 (блокирует старт бэка): спроси `id-engineer`, зачем на практике перенумеровывают выпущенный акт и можно ли менять номер подписанного.

## Указатели

- Этот отчёт: `~/.claude/session-reports/2026-06-20_pto-open-questions-daniil/report.md`
- Vault: `C:\Users\Даниил\Documents\Obsidian Vault`, папка вопросов: `60 - Текущая работа/Открытые вопросы/`
- Память по ИД: `id_volume_graph_built.md`, skill `id-volume-graph`; доменный агент `id-engineer`.
- Учёт: `90 - Учёт работы/README.md` + `Часы по неделям/2026-W19.md`.
